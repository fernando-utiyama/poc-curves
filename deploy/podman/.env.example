# Copie para .env (git-ignorado) para sobrescrever os padrões do ambiente local.
# Credenciais de ambiente descartável — nunca reaproveitar fora daqui.

MSSQL_SA_PASSWORD=CurvasP0c!Local
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
