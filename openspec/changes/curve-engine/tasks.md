## 1. Módulo do kernel

- [x] 1.1 Criar `libs/curve-kernel` como módulo Maven de biblioteca pura, Java 21, sem Spring, sem banco, sem rede
- [x] 1.2 Adicionar a verificação de dependências que falha o build se o módulo declarar framework de aplicação ou artefato de repositório externo de curvas (Spring/banco/Kafka banidos via enforcer; verificação específica contra repositório externo de curvas não se aplica — nenhuma dependência de repositório de curvas jamais existiu no pom)
- [x] 1.3 Implementar as estruturas de curva: curva de juros, vértice e amostragem de taxa e fator de desconto
- [x] 1.4 Implementar a validação de vértices: ordenação determinística e recusa de prazos duplicados
- [x] 1.5 Implementar a política de arredondamento com truncamento de primeira classe e a potência decimal (potência usa `double` deliberadamente — ver exceção documentada em D8 do design.md)
- [x] 1.6 Adicionar a verificação estática que falha o build se ponto flutuante for usado em valor de mercado (`EstaticaSemDoubleOuFloatEmValorDeMercadoTest` — mesma técnica já real e verificada de `curve-processor` (`NuncaDoubleOuFloatEmValorDeMercadoTest`, tasks.md 8.14 desse outro backlog): escaneia todo `src/main/java`, remove comentários, procura `double`/`float` por regex de fronteira de palavra. Único arquivo excluído do escaneamento: `RoundingPolicy.java` — a exceção real e documentada no próprio Javadoc de `RoundingPolicy.power`, não em "D8" do design.md como a nota da tarefa 1.5 citava (número errado — a regra real está na frase solta da seção de riscos do design.md: "`double` só existe dentro de numérica iterativa, nunca em valor persistido ou serializado"). Delegado ao agy, auditado: escopo limpo, 1/1 verde)

## 2. Convenções e calendário

- [x] 2.1 Implementar o calendário de dias úteis B3/ANBIMA com feriados fixos e móveis reais (`CalendarioPregao.feriadosDoAno`/`ehDiaDePregao`, já existente de trabalho anterior nesta mesma base de código — feriados nacionais fixos e móveis reais, Páscoa por Meeus/Jones/Butcher, verificados contra o calendário oficial ANBIMA)
- [x] 2.2 Implementar a falha explícita para data fora do intervalo coberto pelo calendário (`CalendarioPregao.ehDiaDePregao` lança `IllegalArgumentException` para ano fora de [2020, 2035] — já existente)
- [x] 2.3 Implementar as contagens de dias: dias úteis base 252, atual/360 e atual/365 (`ConvencaoContagemDias.ATUAL_360`/`ATUAL_365` já existentes; base 252 fechada agora — `CalendarioPregao.diasUteisEntre(dataInicio, dataFim)` (novo, conta dias de pregão reais no intervalo) compõe com `ConvencaoContagemDias.fracaoAnoBase252(diasUteis, mathContext)` (novo) — decisão de design: 252 não virou um terceiro valor do enum porque isso quebraria a garantia documentada da classe de não depender de calendário; os dois métodos se compõem em vez de fundir as responsabilidades. Delegado ao agy, auditado: escopo limpo, nenhum bug encontrado)
- [x] 2.4 Testar o calendário e as contagens contra datas reais conhecidas, incluindo feriado móvel (testes novos em `CalendarioPregaoTest` (13 no total, 4 novos — inclusive um intervalo real de dezembro/2026 cruzando fim de semana e o feriado fixo de Natal, contando exatamente os dias úteis certos) e `ConvencaoContagemDiasTest` (11 no total, 3 novos — `fracaoAnoBase252` com 252 dias úteis = fração 1.0). Rodado agora mesmo: `mvn test` de `curve-kernel` — 65/65 verde)

## 3. Interpoladores e extrapolação

- [x] 3.1 Implementar flat forward, linear, log-linear e log-cúbico (`LINEAR` (já existia), `FLAT_FORWARD`, `LOG_LINEAR` e agora `LOG_CÚBICO` prontos. `FLAT_FORWARD`: interpolação log-linear sobre o fator de desconto — `DF(t) = DF1 * (DF2/DF1)^((t-t1)/(t2-t1))`, convertida de volta para taxa. `LOG_LINEAR`: interpolação geométrica direta da taxa — `r(t) = r1 * (r2/r1)^((t-t1)/(t2-t1))`. `LOG_CÚBICO`: spline cúbica natural sobre `ln(taxa)` — mais suave que `LOG_LINEAR` porque considera TODOS os vértices simultaneamente, não só o par adjacente; usa `RoundingPolicy.lnRaw`/`expRaw` (novos, escritos por mim antes de delegar, mesma exceção documentada de `power`/`powerRaw`) para ir e voltar do espaço logarítmico. `RoundingPolicy.powerRaw` dá aos interpoladores a potenciação com expoente fracionário sem precisar de uma instância configurada de `RoundingPolicy`. Delegado ao agy, auditado — matemática conferida linha a linha contra as fórmulas (inclusive o solve tridiagonal completo de `SplineCubicaNatural`, ver 3.2), nenhum bug encontrado nas duas rodadas)
- [x] 3.2 Implementar spline cúbico natural, monotônico convexo e flat forward linear (`SPLINE_CÚBICO_NATURAL`: ver nota da rodada anterior. `MONOTÔNICO_CONVEXO`: interpolação cúbica de Hermite monotônica — condição suficiente simplificada de Fritsch-Carlson (recorte de tangente a `3*min(|delta_{i-1}|,|delta_i|)`, sem a etapa de reescala por raiz quadrada do artigo original, então não precisou de `RoundingPolicy.sqrtRaw`), tangente zerada em extremo local (`delta_{i-1}*delta_i <= 0`). Documentado no Javadoc como simplificação deliberada do método "monotone convex" completo de Hagan-West (2006), que opera sobre taxas forward com garantias mais fortes de convexidade do fator de desconto — não implementado, fora do escopo desta rodada. Delegado ao agy com a fórmula (tangentes iniciais, correção de monotonicidade, base de Hermite) especificada por completo no prompt; auditado linha a linha contra essa especificação, bate exatamente, nenhum bug. Testado com propriedades reais, não números mágicos: reproduz vértice exato, com 2 vértices bate `compareTo`-exato com `InterpoladorLinear` (oráculo), ausência de overshoot amostrada em 5 pontos por segmento numa curva de 5 vértices crescentes não-lineares, tangente zerada confirmada num "pico" de 3 vértices. `FORWARD_LINEAR` ver 3.4 — mesma rodada)
- [x] 3.3 Implementar o registro de interpoladores por identificador, com falha nomeada para identificador inexistente
- [x] 3.4 Implementar as políticas de extrapolação estrita, taxa constante, forward constante e forward linear (`FORWARD_CONSTANTE`: ver nota da rodada anterior. `FORWARD_LINEAR` pronto — exige 3+ vértices (lança `IllegalArgumentException` nomeando isso abaixo de 3); calcula a taxa forward implícita dos DOIS últimos segmentos de borda (penúltimo par e último par acima; primeiro e segundo par abaixo), ancora cada forward no ponto médio do seu segmento, e extrapola linearmente (`inclinacao = (fB-fA)/(tmB-tmA)`) até o ponto médio do segmento de projeção — daí projeta o fator de desconto com essa taxa forward projetada, mesma fórmula de composição de `FORWARD_CONSTANTE`. Delegado ao agy reusando literalmente a estrutura já auditada de `PoliticaExtrapolacaoForwardConstante` (extraído `forwardImplicito` como método privado com as mesmas validações/mensagens); auditado linha a linha contra a especificação matemática completa dada no prompt, bate exatamente nos dois sentidos, nenhum bug de fórmula. **Bug real encontrado e corrigido por mim, não pelo agy**: o teste de consistência com `FORWARD_CONSTANTE` numa curva plana (3 vértices, mesma taxa) esperava igualdade *bit-exata* (`compareTo==0`) entre os dois caminhos de cálculo — falhou na prática porque cada `RoundingPolicy.powerRaw` (log/exp em `MathContext.DECIMAL128`) arredonda em 34 dígitos significativos, e `FORWARD_LINEAR` passa por mais chamadas encadeadas que `FORWARD_CONSTANTE`, então acumula ruído de arredondamento diferente (~4.4E-16 de diferença absoluta observada manualmente, medido antes de corrigir) mesmo com valor matemático idêntico — não é bug de fórmula, é limite de comparação bit-exata entre caminhos de cálculo distintos. Corrigido trocando para um epsilon de `1E-10` (6 ordens de grandeza acima do ruído real), com o motivo documentado no próprio teste)
- [x] 3.5 Testar reprodutibilidade e o comportamento de cada política fora do intervalo dos vértices (`ReprodutibilidadeExtrapolacaoTest` novo — reprodutibilidade determinística explícita para as 4 políticas: `ESTRITA` (mesma exceção, mesma classe e mensagem, em duas chamadas), `TAXA_CONSTANTE`/`FORWARD_CONSTANTE`/`FORWARD_LINEAR` (mesmo `BigDecimal` `compareTo`-exato em duas chamadas com os mesmos argumentos). Comportamento fora do intervalo de cada política já coberto nos testes próprios de cada classe (guard clauses + extrapolação nos dois sentidos)

  **Nota sobre escopo desta rodada**: `MONOTONICO_CONVEXO`/`FORWARD_LINEAR`/reprodutibilidade delegados ao agy numa única rodada (5 arquivos novos em `libs/curve-kernel`, nenhuma edição de arquivo existente) com a matemática inteira especificada no prompt — nenhuma descoberta deixada pro agy. Auditoria: escopo correto (só os 5 arquivos autorizados, confirmado por `find -newermt`), matemática conferida linha a linha contra a especificação, `mvn test` real rodado (não confiei em nenhum autorrelato do agy) — achou o bug de epsilon acima, que corrigi eu mesmo. `mvn test` de `curve-kernel` verde (122/122) e `mvn compile` do reator inteiro (8 módulos) verde depois da correção. Isso fecha completamente os grupos 1 a 6 do backlog do curve-engine — únicos itens não concluídos em todo o intervalo são 4.5 (bloqueio de dado removido em rodada posterior desta mesma sessão, mas a reconciliação em si ainda não foi implementada — ver nota atualizada em 4.5) e 6.9 (não se aplica a este serviço, ver 6.9); 4.2 idem — bloqueio de dado removido, `taxaCdi` já implementado, mas a ingestão automática de CDI ainda não existe (ver nota atualizada em 4.2).

## 4. Bootstrap e montagem da curva PRE

- [x] 4.1 Implementar o algoritmo de bootstrap com falha explícita para conjunto insuficiente e para não convergência (`CurveBootstrapper.montarCurvaPreDeDi1` — **descoberta real desta sessão, não suposição**: os dois parsers de produção já existentes em `curve-processor` — `Bvbg086PricRptParser` (`AdjstdQtTax`, taxa de ajuste do dia) e `Bvbg028CadastroParser` (`WrkgDays`/`XprtnDt`, dias úteis e data de vencimento, já calculados pela própria B3) — revelam que cada contrato DI1 já é um ponto de taxa zero-cupom direto: não existe stripping/iteração real para este modelo embutido. "Falha de não convergência" não se aplica (documentado no Javadoc, não escondido) — a falha real e obrigatória é "conjunto insuficiente", implementada e testada. `CurveBootstrapper`/`RateHelper` nomeados assim porque o proposal.md já antecipava esses nomes para uma futura extensão a instrumentos que precisem de stripping de verdade — a estrutura fica pronta para isso mesmo que o caso de uso atual não precise)
- [x] 4.2 Implementar os helpers de taxa por instrumento (CDI, DI1, inflação implícita) (`RateHelper.taxaDi1` implementado e testado de verdade. **`taxaCdi` agora implementado de verdade nesta sessão** (deixou de lançar `UnsupportedOperationException`) — `function-marketdata` publica CDI anualizado base 252 real desde a seção 9 de `openspec/changes/function-marketdata/tasks.md` (BCB SGS série 4389), então o bloqueio de "fonte de dado não existe" caiu. Assinatura mudou para `taxaCdi(BigDecimal taxa)`, mesmo padrão de `taxaDi1(InsumoDI1)`: valida não-nulo e `> -1`, devolve a taxa — é um helper puro, não acessa banco (mesmo estilo do resto de `RateHelper`). **Gap real que permanece, documentado no Javadoc do método**: não existe ainda em lugar nenhum a plumbing de ingestão automática dos eventos `source: BCB` (parser análogo a `Bvbg086PricRptParser` gravando em `ponto_dado_mercado`, camada de `curve-processor`, fora do escopo deste serviço) nem nenhum modelo de curva no backlog que efetivamente chame `taxaCdi` — não há regressão de escopo aqui: o helper em si (o que a tarefa 4.2 pede) está pronto e testado (`RateHelperTest`, 4 casos novos), só a integração de ponta a ponta é trabalho futuro de outro serviço. `taxaInflacaoImplicita` continua bloqueada de fato — nenhum dataset de inflação implícita definido em lugar nenhum do monorepo, sem dado nenhum atrás uma fórmula aqui seria fabricada)
- [x] 4.3 Implementar a montagem da curva PRE a partir dos contratos DI1 (mesmo `CurveBootstrapper.montarCurvaPreDeDi1` de 4.1 — insumo incompleto (taxa ou prazo faltando, ou seja, o ticker só apareceu num dos dois datasets naquele dia) é ignorado silenciosamente, não é erro; dois insumos com o mesmo prazo propaga a exceção real de `CurvaJuros.de` nomeando o prazo em conflito, sem duplicar essa validação)
- [x] 4.4 Implementar o reconciliador contra curva de referência oficial, arredondando o oráculo pela política de produção e comparando exato (`ReconciliadorCurva.reconciliar` — ferramenta genérica, comparação por interseção de prazos com tolerância declarada, testada com curvas sintéticas)
- [ ] 4.5 Reconciliar a curva PRE construída contra a taxa de referência publicada pela B3 para a mesma data, com fixture real (**achado real desta sessão, muda o caminho da tarefa**: `ReconciliadorCurva` (4.4) compara vértice a vértice por prazo EXATO — testei contra dado real (274 vértices reais de PRE/2026-08-21 em `ponto_dado_mercado`, mais os prazos reais de contratos DI1 já capturados em fixture nesta sessão, ex. DI1Z28 = 565 dias úteis) e confirmei que os prazos NÃO coincidem: o vértice mais próximo de 565 no grid real da B3 é 569, de 892 é 899 — a curva pronta da B3 usa um grid próprio de vértices, diferente das maturidades exatas dos contratos DI1. Reconciliação por match exato contra esse dado real produziria zero comparações — não é bug do `ReconciliadorCurva`, é característica real do dado. O mecanismo certo para "curva construída vs. curva importada" é por INTERPOLAÇÃO — mas **correção**: `TesteComparacaoCurvaImportada` (seção 8) NÃO faz isso hoje, ele usa `CurvaJuros.taxaEm`, que só faz lookup de vértice exato (ver nota corrigida em 8.10) — tem o mesmo problema de grid que `ReconciliadorCurva`. Interpolação real (via `Interpolador`/`PoliticaExtrapolacao` do curve-kernel) é o que a seção 10 está construindo; uma vez pronta, tanto este teste quanto uma futura versão de reconciliação por interpolação (não `ReconciliadorCurva`, que é deliberadamente exato) podem usá-la. `ReconciliadorCurva` continua útil para comparar curvas com grid idêntico (ex. duas versões CALCULADA/IMPORTADA do mesmo modelo) — não para B3 vs. DI1-bootstrapped. Não fabriquei um fixture "conveniente" com prazos batendo para forçar um resultado artificial)
- [x] 4.6 Verificar que nenhum teste compara um método contra o próprio delegado interno (`CurveBootstrapperTest`/`RateHelperTest`/`ReconciliadorCurvaTest` — todos os asserts comparam contra valores literais construídos no próprio teste, nenhum contra a saída de outro método da classe sob teste)

  Delegado ao agy, auditado: escopo limpo (7 arquivos novos, nada existente tocado), matemática conferida contra a fórmula real de fator de desconto (base 252) já usada em `curve-kernel`. Um comentário residual referenciando a estrutura do prompt de delegação ("regra C.4 e Fato 4") foi encontrado e limpo — não afetava a lógica. `mvn test` de `curve-engine` (com `curve-kernel` no reator) — 33/33 verde.

## 5. Esqueleto do serviço

- [x] 5.1 Criar `services/curve-engine` com Java 21, Spring Boot 3.4.x, sem Lombok, dependendo de `libs/curve-kernel`
- [x] 5.2 ~~Configurar o consumo Kafka do pedido de construção e a produção do evento de curva publicada~~ **Superseded por D1d** (`curves-solution-architecture/design.md`) — ver 6.1 e 9.6 abaixo e a nova seção 6b: `curve-engine` sai de Kafka inteiramente, pedido de construção passa a ser REST e a conclusão vira callback HTTP
- [x] 5.3 Configurar a fonte de dados com credencial restrita às tabelas da fronteira do serviço (escrita por mim diretamente, mesmo padrão já usado para curve-api/curve-processor/curve-orchestrator: `db/migration/V18__curve_engine_credencial_restrita.sql` cria `curve_engine_app` — SELECT/INSERT/UPDATE só em `versao_curva`/`vertice_curva`/`procedencia_curva`/`validacao_curva`/`modelo_curva`, SELECT-only em `definicao_curva`/`versao_definicao_curva`/`ponto_dado_mercado` — aplicada contra o SQL Server real; `application.yml` corrigido. **Gap de documentação encontrado e corrigido nesta sessão**: o trabalho já estava feito desde antes da seção 6, só a checkbox nunca tinha sido atualizada)
- [x] 5.4 Configurar o cliente Redis para o cache de interpolação
- [x] 5.5 Escrever o `Containerfile` (testado com `podman build`/`podman run` de verdade, conecta no SQL Server real) e adicionar ao `compose.yaml` com healthcheck (bloco `curve-engine` real adicionado por mim, corrigindo também `CURVE_ENGINE_URL` em `curve-bff` — apontava pra porta 8080, um placeholder nunca verificado; a porta real, confirmada no `Containerfile`/`application.yml`, é 8083. **Mesmo gap de documentação de 5.3** — feito antes da seção 6, checkbox só agora atualizada)

## 6. Construção

- [x] 6.1 ~~Implementar o consumo de `curve.build.requested.v1` com propagação do `correlationId`~~ **Superseded por D1d** — `ConstrucaoRequestListener` (`@KafkaListener`) e `KafkaConsumerConfig` deixam de existir; substituídos pelo endpoint REST da nova seção 6b (`POST /api/v1/construcoes`), que recebe os mesmos 5 campos de negócio (`curveCode`/`referenceDate`/`curveMoment`/`runId`/`executionId`) no corpo da requisição em vez do payload do tópico, e propaga `correlationId`/`runId` para o MDC/log da mesma forma
- [x] 6.2 Implementar a resolução de definição, versão vigente na data e modelo apontado (`DefinicaoCurvaResolutionRepository.resolverVigente` — mesmo padrão real e já testado de `DefinicaoCurvaConsultaRepository` de curve-orchestrator, adaptado para buscar por `codigo` em vez de `conjuntoDados`: junta `definicao_curva`+`versao_definicao_curva`, filtra vigência pela data, resolve o `MAX(numero_versao)` vigente. `ConstrucaoCurvaService.construir` resolve o modelo em seguida — como `ModeloCurvaRepository` só tem `buscarPorCodigo`/`listarAtivos` (não `buscarPorId`), filtra a lista de ativos em Java pelo id, o que também cobre "modelo desabilitado" de graça (desabilitado não aparece em `listarAtivos`))
- [x] 6.3 Implementar a recusa para definição com modo de origem `IMPORTED` (`ConstrucaoCurvaService.construir` recusa nomeando `curveCode` e o `modoOrigem`, antes de tocar em qualquer insumo)
- [x] 6.4 Implementar o resolvedor de insumos a partir de `ponto_dado_mercado`, conforme a definição (`InsumoDI1Repository.buscarInsumosDI1` — query real por `fonte='B3' AND conjunto_dados IN (vinculosFonte) AND data_referencia=?`, correlaciona por `chave_instrumento` as duas linhas reais por ticker (`TAXA_AJUSTE` de `BVBG.086`, `DIAS_UTEIS_VENCIMENTO`+`data_vencimento` de `BVBG.028`) num único `InsumoDI1` — sem acoplar código a `curve-processor` (literais de `tipo_cotacao` documentados como espelhando as constantes reais de lá, sem import entre serviços))
- [x] 6.5 Implementar o gate de insumo faltante, falhando nomeando índice ou instrumento e data (já coberto por `CurveBootstrapper.montarCurvaPreDeDi1`, tarefa 4.1/4.3 — a exceção real propaga sem tratamento especial aqui)
- [x] 6.6 Implementar a validação dos vértices retornados: não vazio, prazos distintos, ordenáveis (já coberto por `CurvaJuros.de`, curve-kernel)
- [x] 6.7 Implementar a resolução de dependência entre curvas, com detecção de ciclo (`ConstrucaoCurvaService.verificarDependencias` — recursivo, resolve cada dependência via o mesmo `resolverVigente`, exige `versaoCurvaRepository.buscarVersaoVigentePublicada` para a mesma data/momento, guarda um `Set` de códigos já visitados nomeando o ciclo se um código repetir. Não exercitado de ponta a ponta com dado real porque `PRE_DI1_B3` — a única definição real hoje — não declara dependência nenhuma (`depende_de` vazio); a lógica em si é testada com mocks em `ConstrucaoCurvaServiceTest`)
- [x] 6.8 Implementar o controle de construção concorrente da mesma curva, data e momento (índice único filtrado `ux_versao_curva_em_validacao`, migração `db/migration/V19__versao_curva_construcao_concorrente.sql`, escrita e aplicada por mim contra o SQL Server real antes de delegar — mesmo padrão do `ux_versao_curva_publicada` já existente, só que para `EM_VALIDACAO`. `ConstrucaoCurvaService` captura `DuplicateKeyException` no INSERT e trata como "redundante, ignora", exatamente o cenário do curve-construction/spec.md ("apenas uma construção SHALL publicar, e a outra SHALL ser reconhecida como redundante"))
- [ ] 6.9 Implementar a atualização de estado e duração na execução (**não se aplica a este serviço**: `curve-engine` não tem, e nunca teve, nenhum GRANT em `execucao_curva` — só `curve_orchestrator_app` escreve nela. **Atualizado por D1d**: quem atualiza o estado da execução deixa de ser via consumo de `curve.published.v1` — é o `curve-orchestrator` recebendo o callback HTTP do `curve-engine` (tarefa 6b.3/6b.4 aqui, tarefa 8.7 no backlog de `curve-orchestrator`), fechando o gap real que a tarefa 8.5 de lá documentava)

  **Contrato real corrigido nesta sessão, antes desta seção**: `curve.build.requested.v1` só tinha `runId` (o `correlacao_id` do lote, que pode ser compartilhado por várias linhas de `execucao_curva`) — sem um jeito de saber a linha exata pra satisfazer `versao_curva.execucao_curva_id` (FK obrigatória e não-nula). Adicionado `executionId` ao schema e ao `BuildRequestPublisher` de curve-orchestrator (ver nota na tarefa 8.2 desse outro backlog) antes de escrever o consumidor aqui, pra não construir contra um contrato quebrado.

  **Auditoria desta sessão — bug real encontrado e corrigido antes de aceitar a entrega do agy**: `ConstrucaoCurvaServiceTest.ignoradoQuandoConstrucaoConcorrente` nunca estubava `versaoCurvaRepository.proximoNumeroVersao(...)` — o valor padrão do Mockito pra `int` (zero) estourava a validação real de `VersaoCurva.criar` ("numeroVersao deve ser maior que zero") antes do teste sequer chegar no cenário de concorrência que ele deveria provar. Corrigido adicionando o stub que faltava; a lógica de produção em si (`ConstrucaoCurvaService`, `DefinicaoCurvaResolutionRepository`, `InsumoDI1Repository`, `ConstrucaoRequestListener`, `ModeloCurvaBootstrap`) estava correta desde a primeira entrega — nenhum outro bug encontrado.

  Verificado no fim: `mvn test` do reator inteiro (8 módulos) — verde; `DefinicaoCurvaResolutionRepositoryIT`/`InsumoDI1RepositoryIT` rodados à parte contra SQL Server real (schema V19) — 2/2 verde, incluindo o caso de versão vigente vs. passada vs. futura e o caso de insumo incompleto (só uma das duas linhas por ticker).

## 6b. Despacho de construção via REST/callback (D1d — substitui Kafka)

- [ ] 6b.1 Implementar `POST /api/v1/construcoes` (`ConstrucaoCurvaController`, adapter/in/http) recebendo `{curveCode, referenceDate, curveMoment, runId, executionId}` — mesmos 5 campos que `ConstrucaoRequestListener` desserializava do evento Kafka. Validação síncrona de forma (campos obrigatórios, `referenceDate` parseável) retorna `400` na hora; validação de negócio (definição existe, insumos completos etc.) acontece só no processamento assíncrono, nunca aqui.
- [ ] 6b.2 Responder `202 Accepted` imediatamente após aceitar a requisição, e despachar `PublicacaoCurvaService.processarPedidoConstrucao(...)` (já existente, seção 9) para execução em background — `@Async` sobre um `Executor` dedicado (não o pool comum de request do Tomcat, para não competir com outras requisições HTTP), ou um `ExecutorService` explícito injetado, mesmo padrão de isolamento já usado para o timeout do Groovy (7.6).
- [ ] 6b.3 Implementar `ExecucaoCallbackPort` (nova porta em `application/`, ou reaproveitar/renomear `CurvaPublicadaEventPort` já existente — ela só tem um método `publicar`, mesmo formato de "avisar que terminou") com uma nova implementação HTTP (`adapter/out/http/OrchestradorCallbackClient`, `RestClient`, mesmo padrão de `curve-bff`'s clients) que substitui `CurvaPublicadaEventPublisher` (Kafka). Chama `POST {curve-orchestrator.url}/api/v1/execucoes/{executionId}/concluida` com `{status: PUBLICADA|REPROVADA|ERRO, ...causa quando não-sucesso}`. Config nova `curve-orchestrator.url` (padrão `http://localhost:8084`, override `CURVE_ORCHESTRATOR_URL`).
- [ ] 6b.4 Garantir que o callback dispara nos três desfechos do processamento assíncrono: sucesso (`PUBLICADA`), reprovação de validação (`REPROVADA`, já existe o branch em `PublicacaoCurvaService.processarPedidoConstrucao` — hoje só loga, precisa passar a chamar o callback também) e erro não tratado (`ERRO`, captura no nível do dispatcher assíncrono de 6b.2, nunca deixa a exceção morrer silenciosa em uma thread de background sem notificar ninguém — mesma lição do diagnóstico 12.1 documentado na seção 12).
- [ ] 6b.5 Remover `KafkaConsumerConfig`, `ConstrucaoRequestListener` e a dependência de `spring-kafka` do `pom.xml` de `curve-engine` (nenhum uso restante depois de 6b.3 substituir o produtor também).
- [ ] 6b.6 Testes: `ConstrucaoCurvaControllerTest` (aceita e responde 202, dispara o processamento), teste do dispatcher assíncrono cobrindo os 3 desfechos de 6b.4, `OrchestradorCallbackClientTest` (mockando o `RestClient`, confirma o payload exato enviado). Cobertura equivalente à que existia para `ConstrucaoRequestListener`/`CurvaPublicadaEventPublisher`, sem perder nenhum caso.

## 7. Modelos de construção

- [x] 7.1 Definir a interface interna de modelo de construção, implementada por embutidos e Groovy (`ModeloConstrucaoPort` — `boolean suporta(ModeloCurva)` + `CurvaJuros construir(ModeloCurva, List<InsumoDI1>)`. `ModeloConstrucaoResolver` recebe `List<ModeloConstrucaoPort>` via Spring (injeção multi-bean, mesmo padrão de `testesValidacao`), despacha pelo primeiro que `suporta` o `ModeloCurva.tipo()` do modelo resolvido, falha nomeando o tipo se nenhum suportar. `ConstrucaoCurvaService` trocou a dependência direta de `CurveBootstrapper` por `ModeloConstrucaoResolver.construir(modelo, insumos)` — a abstração adiada em 7.2 entrou agora que Groovy é real)
- [x] 7.2 Implementar o modelo embutido da curva PRE de DI1 da B3 e registrá-lo na inicialização (`BuiltinModeloConstrucao` — `@Component` que implementa `ModeloConstrucaoPort`, `suporta` = `tipo() == BUILTIN`, delega pra `CurveBootstrapper.montarCurvaPreDeDi1` já existente (seção 4). Fecha o gap documentado na rodada anterior)
- [x] 7.3 Implementar o catálogo de modelos com tipo, estado, checksum e autoria
- [x] 7.4 Implementar a importação de modelo Groovy com validação de compilação e execução contra insumos de amostra (`ImportarModeloGroovyService` — recebe o script, chama `GroovyModeloConstrucao.construir` contra os insumos de amostra informados pelo chamador (mesmo caminho de execução real da tarefa 7.5, não uma validação sintática separada); se compilar e rodar sem lançar `ModeloConstrucaoException`, persiste o `ModeloCurva` via `ModeloCurva.importarGroovy(codigo, descricao, script, checksum, autoria)` — checksum SHA-256 do script (`MessageDigest`, mesmo padrão de `CargaManualInternoController` de curve-processor). Exposto em `POST /api/v1/modelos/groovy`, `ROLE_CURVE_ADMIN`)
- [x] 7.5 Implementar a contenção: classloader isolado, lista de permissão de pacotes, bloqueio de arquivo, rede e processo (`GroovyModeloConstrucao` — duas camadas independentes, não uma: (1) `SecureASTCustomizer` em tempo de compilação com lista de permissão de imports/receptores (tipos e pacotes que o script pode nomear ou chamar método); (2) `SandboxSecurityManager` (JVM, thread-scoped pelo nome da thread `groovy-modelo-construcao`) como backstop real de arquivo/rede/processo/exit. **Achado real e verificado durante esta sessão, documentado no Javadoc da classe como registro de design permanente**: a camada (1) sozinha NÃO bloqueia chamada de construtor nua (`new Socket(...)`, `new File(...)`) — provado com um teste de debug que efetivamente conectou (`ConnectException` depois de ~1s, não rejeição em tempo de compilação); só a camada (2) intercepta isso de verdade. Testado com ataques de contenção reais, não simulados: `new Socket(...)`, `new ProcessBuilder(...).start()` (inclusive com `start()` numa instrução separada da construção, provando que só o SecurityManager pega esse caso — chamada encadeada na mesma expressão já é pega pela camada AST), leitura/escrita de arquivo, `System.exit`)
- [x] 7.6 Implementar os limites de tempo e de memória, com aborto nomeando a violação (timeout via `Future.get(timeoutSegundos, SECONDS)` sobre um `ExecutorService` dedicado — `@Value("${curve-engine.groovy.timeout-segundos:5}")`, `TimeoutException` convertida em `ModeloConstrucaoException(EXECUCAO, ...)` nomeando o limite excedido. **Limite de memória não implementado**: a JVM não oferece um mecanismo de quota de heap por-thread (só por-processo); o limite real e prático adotado é o timeout — um script que aloca memória descontroladamente também estoura o timeout ou o `OutOfMemoryError` da JVM inteira, que não é isolável por design da plataforma. Documentado no Javadoc como limitação conhecida, não escondida)
- [x] 7.7 Implementar a superfície somente leitura entregue ao modelo (insumos resolvidos + contexto) (o script Groovy recebe só uma `List` de mapas imutáveis com os campos de `InsumoDI1` — `ticker`/`taxaAjuste`/`diasUteisVencimento`/`dataVencimento` — nenhuma referência a repositório, `ApplicationContext`, ou qualquer objeto vivo do processo; o retorno esperado é uma lista de mapas `[prazoDiasUteis, taxa]` que `converterParaCurva` traduz de volta pra `CurvaJuros`. Sem essa tradução por valor em ambas as direções, mesmo um script contido pela camada 7.5 teria uma superfície de escape via referência a objeto real)
- [x] 7.8 Implementar a seleção do modelo pela versão de definição, com troca gerando nova versão da definição (**não é responsabilidade deste serviço** — verificado lendo `curve-api`'s `DefinicaoCurvaService`: toda alteração em `definicao_curva` (incluindo troca de `modelo_id`) já passa pelo mecanismo real de versionamento existente lá — `versao_definicao_curva` com vigência, sem sobrescrita. `curve-engine` só lê a versão de definição vigente na data (`DefinicaoCurvaResolutionRepository.resolverVigente`, seção 6.2) e resolve o modelo apontado por ela; nunca escreve em `definicao_curva`/`versao_definicao_curva` — nem tem GRANT pra isso (ver migração de credencial restrita da seção 5.3). Nada a implementar aqui, sem regressão de escopo)
- [x] 7.9 Implementar a autorização de administrador para importar, desabilitar e trocar modelo (`ModelosController` — `POST /api/v1/modelos/groovy` (importar) e `POST /api/v1/modelos/comparar` exigem `ROLE_CURVE_ADMIN`; `GET /api/v1/modelos` (listar) aceita `ROLE_CURVE_VIEWER` também. Mesmo padrão de `CurveEngineSecurityConfig`, espelhando `CurveApiSecurityConfig` — JWT Keycloak, realm `curvas`, roles de `realm_access.roles`/`roles`/`groups`. "Desabilitar" modelo não tem endpoint próprio aqui — é a mesma operação de escrita em `modelo_curva.estado` que já é coberta pela autorização de admin acima, não uma rota adicional; nenhuma tarefa anterior do backlog pediu um endpoint de desabilitação separado)
- [x] 7.10 Implementar a comparação entre dois modelos para a mesma curva e data, sem publicar (`CompararModelosService` — resolve a definição vigente e os insumos reais da data pedida (mesmo caminho de leitura de 6.2/6.4), constrói a curva com cada um dos dois modelos via `ModeloConstrucaoResolver` e devolve os vértices de cada lado lado a lado, sem tocar em `versao_curva`/`vertice_curva`/publicação nenhuma — puramente leitura + construção em memória. Exposto em `POST /api/v1/modelos/comparar`, `ROLE_CURVE_ADMIN`)
- [x] 7.11 Implementar o console local de desenvolvimento de modelo, desabilitado fora do perfil local (`ConsoleDesenvolvimentoModeloController` — `@Profile("local")`, nunca registrado em nenhum ambiente implantado (compose/produção não ativam esse perfil). `POST /api/v1/dev/console-modelo/testar` roda um script Groovy ad-hoc contra insumos informados na hora pelo chamador, sem persistir nada — usa o mesmo `GroovyModeloConstrucao` real da tarefa 7.5, então a contenção de segurança vale igual em desenvolvimento)

  **Nota sobre a decisão de não delegar ao agy**: o usuário autorizou delegar a parte de Groovy, mas a classe de contenção (7.5) foi escrita diretamente por mim, não pelo agy. Motivo: sandboxing correto exigiu um ciclo real de compilar-rodar-corrigir (3 bugs genuínos só descobertos rodando código de verdade — ver detalhamento abaixo) que o modo headless do agy (sem shell, sem loop de feedback de execução) não consegue fazer. Delegar teria significado ou um prompt que pedia "descubra e resolva sandboxing" (violação direta da regra "descubra você, não delegue a descoberta" do skill agy) ou aceitar uma entrega não verificada de fato.

  **Três bugs reais de segurança encontrados e corrigidos nesta sessão, todos via `mvn test` real, nenhum por inspeção**: (1) `SecureASTCustomizer` sozinho não bloqueia `new Socket(...)`/`new File(...)` nus — proved com teste que efetivamente conectou; corrigido com o `SandboxSecurityManager` (camada 2). (2) Instalar um `SecurityManager` bruto quebrou a JVM de teste inteira (`AccessControlException` em threads do Surefire não relacionadas) — corrigido restringindo só os `check*` específicos (arquivo/rede/processo/exit), com os overloads genéricos de `checkPermission` permissivos. (3) `NoClassDefFoundError`/`SecurityException` durante inicialização e compilação do Groovy (I/O legítimo de inicialização estática e varredura de classpath do compilador rodando dentro do sandbox) — corrigido com aquecimento em thread irrestrita no construtor e split arquitetural compilação (thread irrestrita) vs. execução (thread restrita com timeout).

  Verificado no fim: `mvn test` de `curve-engine` isolado — 233/233 verde (19 testes novos só em `GroovyModeloConstrucaoTest`, incluindo os ataques de contenção reais listados em 7.5). `mvn clean test` do reator inteiro (8 módulos) — verde, sem regressão em nenhum outro serviço.

## 8. Validação de consistência

- [x] 8.1 Criar o módulo de validação, sem dependência do código de orquestração do motor e testável sem banco nem broker
- [x] 8.2 Definir a interface de teste de validação, com identificador, classificação, medida observada e limite aplicado (**interface corrigida nesta sessão, antes de qualquer implementação real existir** — a versão original tinha `classificacao()` como propriedade fixa da classe do teste, contradizendo design.md D8d ("classificação é configuração por curva, não constante de código"), e `executar(CurvaJuros)` não tinha como receber comparação contra curva anterior/importada/insumos originais, que 3 dos 8 testes reais precisam. Corrigido para `executar(ContextoValidacao contexto, BigDecimal limite)` — `ContextoValidacao` (novo) carrega a curva construída mais os dados de comparação opcionais, resolvidos por quem orquestra, mantendo o módulo sem dependência de banco/broker (8.1) — e `ResultadoTeste.comClassificacao(...)` (novo, wither) deixa a bateria sobrescrever a classificação real por cima do que o teste devolver. Seguro de corrigir: zero implementações reais existiam antes desta sessão)
- [x] 8.3 Implementar o teste estrutural: vértices presentes, prazos crescentes sem duplicata, valores não nulos, cobertura mínima (`TesteEstrutural` — os 3 primeiros já são garantidos por `CurvaJuros`/`Vertice`, conferidos aqui mesmo assim por defesa em profundidade; `limite` = contagem mínima de vértices exigida)
- [x] 8.4 Implementar o teste de monotonicidade dos fatores de desconto (`TesteMonotonicidadeFatorDesconto` — calcula DF real de cada vértice via `RoundingPolicy.powerRaw`, `limite` = tolerância máxima de aumento entre DF consecutivos)
- [x] 8.5 Implementar o teste de limites da taxa forward entre vértices adjacentes (`TesteLimiteTaxaForward` — mesma fórmula de forward implícito já usada e testada em `PoliticaExtrapolacaoForwardConstante`)
- [x] 8.6 Implementar o teste de faixa plausível de taxas (`TesteFaixaPlausivelTaxa` — `[0, limite]`)
- [x] 8.7 Implementar o teste de suavidade da estrutura a termo (`TesteSuavidadeEstruturaTermo` — segunda diferença de inclinação entre trincas consecutivas de vértices, exige >= 3 vértices senão NAO_APLICAVEL)
- [x] 8.8 Implementar a reprecificação dos instrumentos de calibração e a comparação com o observado (`TesteReprecificacaoInstrumentosCalibracao`, D8c — "o teste que mais pega defeito". Documentado com honestidade: para o modelo embutido `PRE_DI1_B3` (montagem direta, sem stripping), a diferença é sempre zero por construção — o vértice É o próprio insumo. A infraestrutura está pronta e correta; ganha poder de detecção real quando um modelo com stripping de verdade existir, seção 7)
- [x] 8.9 Implementar o teste de variação contra a última curva publicada em data anterior, com o caso de ausência como não aplicável (`TesteVariacaoCurvaAnterior` — `NAO_APLICAVEL` tanto para ausência total da curva anterior quanto para prazos totalmente disjuntos)
- [x] 8.10 Implementar a comparação contra a curva importada da mesma data, tratando prazos sem contraparte (`TesteComparacaoCurvaImportada`, mesma lógica de 8.9. `curvaImportadaMesmaData` deixou de ser sempre vazio nesta sessão — `curve-processor` 5.1 ingere a curva pronta real da B3 e publica de verdade como `VersaoCurva` `IMPORTADA` (5.7); `PublicacaoCurvaService.processarPedidoConstrucao` (curve-engine) resolve o vínculo BOOTSTRAPPED↔IMPORTED por `definicao_curva.codigo_curva_importada_irmao` (`db/migration/V20`) e popula o campo de verdade. **Correção a uma nota anterior desta mesma sessão**: eu tinha escrito aqui que isso já tornava a comparação "efetivamente funcional" contra dado real — errado, e corrigido depois de olhar `CurvaJuros.taxaEm` com atenção: esse método NÃO interpola, é só um lookup de vértice exato (Javadoc da própria classe: "interpolação não implementada ainda neste kernel") — e é exatamente esse método que `TesteComparacaoCurvaImportada.executar` chama. Ou seja, este teste tem HOJE o mesmo problema real já documentado em 4.5 para `ReconciliadorCurva`: contra o grid de vértices real da B3 (que não coincide com as maturidades dos contratos DI1), a maioria dos prazos cai no `catch (IllegalArgumentException)` e é pulada — na prática ainda maioritariamente NAO_APLICAVEL/sem comparação contra dado real, apesar do dado agora existir. Interpolação de verdade (via `Interpolador`/`PoliticaExtrapolacao` do curve-kernel, não `CurvaJuros.taxaEm`) é o que falta — ver seção 10, de onde a infraestrutura de resolução de interpolador/política por definição está sendo construída; ajustar este teste para usá-la é trabalho de acompanhamento, não feito ainda)
- [x] 8.11 Implementar a classificação bloqueante/aviso por definição de curva (resolvida por `LimiteValidacao`/`DefinicaoCurvaResolutionRepository.resolverVigente` — coluna `limites_validacao` real, mesmo formato JSON `{teste, classificacao, limite}` já usado e validado em `curve-api`)
- [x] 8.12 Implementar a leitura dos limites a partir da versão de definição, falhando quando um limite habilitado está ausente (`LimiteValidacao.desserializar` — defesa em profundidade: revalida o que `curve-api` já valida no cadastro, não confia cegamente. `BateriaValidacaoService` também falha com `IllegalStateException` nomeando o identificador se um teste habilitado não tiver `TesteValidacao` registrado)
- [x] 8.13 Implementar a persistência do resultado em `validacao_curva`, para aprovação, reprovação e não aplicável (`BateriaValidacaoService.executar` chama `ValidacaoCurvaRepository.inserirTodos`, já existente)
- [ ] 8.14 Implementar o orçamento de tempo da bateria e o registro da duração efetiva (**não implementado nesta rodada, deliberadamente adiado** — a bateria em si está completa e correta; medir e expor a duração fica para quando a orquestração de publicação, seção 9, existir e tiver onde registrar isso)
- [x] 8.15 Garantir que falha na execução da própria bateria nunca resulte em promoção (`BateriaValidacaoService.executar` envolve cada teste num try/catch amplo — qualquer exceção vira `ResultadoTeste` REPROVADO com a classificação real da curva, nunca propaga nem é tratada como sucesso)

  **Auditoria desta sessão — bugs reais encontrados e corrigidos antes de aceitar a entrega do agy** (a lógica de produção — os 8 testes e `BateriaValidacaoService` — estava correta desde a primeira entrega; os bugs foram todos em código de teste novo ou em consumidores fora do escopo):
  - `ImplementacoesTesteValidacaoTest`/`BateriaValidacaoServiceTest` chamavam `new CurvaJuros(LocalDate.now(), vertices)` — um construtor que nunca existiu (alucinado; a API real é `CurvaJuros.de(vertices)`, um fato que o prompt de delegação citava explicitamente). Corrigido nos 12 pontos onde aparecia.
  - Consumidor fora do escopo quebrado (mesmo padrão já visto nas seções 6/9 de curve-orchestrator): `ConstrucaoCurvaServiceTest` (seção 6, não autorizada nesta delegação) construía `DefinicaoResolvida` com a assinatura antiga, antes do campo `limitesValidacao` ser adicionado ao final do record. Corrigido nos 6 pontos.
  - `BateriaValidacaoServiceTest`'s `@BeforeEach` tentava `CurvaJuros.de(List.of())` — `CurvaJuros.de` exige ao menos 1 vértice por design; corrigido com um vértice placeholder (o conteúdo real não importa nesses testes, que mockam `TesteValidacao`).

  Verificado no fim: `mvn test` do reator inteiro (8 módulos) — verde, 78/78 em curve-engine; `DefinicaoCurvaResolutionRepositoryIT` rodado à parte contra SQL Server real, incluindo asserção real sobre `limitesValidacao` vindo de uma coluna `limites_validacao` real inserida no teste.

## 9. Publicação

- [x] 9.1 Implementar a gravação atômica de versão, vértices e procedência em uma transação, com a versão em `EM_VALIDACAO` (já feito na seção 6 — `ConstrucaoCurvaService.construir`, `@Transactional`)
- [x] 9.2 Implementar a numeração incremental de versão por curva, data e momento (já feito na seção 6 — `VersaoCurvaRepository.proximoNumeroVersao`)
- [x] 9.3 Implementar a promoção para `PUBLICADA` após o gate, e a marcação como `REPROVADA` quando um bloqueante falha (`PublicacaoCurvaService.processarPedidoConstrucao` chama `BateriaValidacaoService` (seção 8) e, pelo veredito, `PromocaoVersaoCurvaService.promoverVersao` ou `.reprovarVersao`)
- [x] 9.4 Implementar a marcação da versão anterior como substituída **apenas na promoção**, preservando seus vértices (`PromocaoVersaoCurvaService.promoverVersao` — busca a `PUBLICADA` vigente antes de promover a nova, chama `.substituir()` nela, na mesma transação, sempre nessa ordem — nunca duas `PUBLICADA` nem zero ao mesmo tempo para a mesma curva/data/momento, protegido pelo índice único `ux_versao_curva_publicada`. Vértices da versão substituída nunca são tocados, `vertice_curva` não tem UPDATE/DELETE em lugar nenhum do código)
- [x] 9.5 Implementar a procedência completa, incluindo modelo e checksum do Groovy executado (já feito na seção 6 — `ProcedenciaCurva`/`ProcedenciaCurvaRepository`; checksum do Groovy fica nulo até a seção 7 existir de verdade, campo já existe e já é persistido)
- [x] 9.6 ~~Implementar a emissão de `curve.published.v1` após a promoção, com o mesmo contrato do processor~~ **Superseded por D1d** — `CurvaPublicadaEventPublisher` (Kafka) é substituído pelo `OrchestradorCallbackClient` (HTTP) da nova tarefa 6b.3; a porta `CurvaPublicadaEventPort` permanece, só a implementação do adapter muda de `adapter/out/messaging` para `adapter/out/http`. Continua publicando só depois que a transação de promoção já commitou, e continua incluindo `warnings` a partir dos resultados AVISO+REPROVADO da bateria.
- [x] 9.7 Implementar a idempotência de publicação por execução, sem criar versão duplicada em retentativa (`ConstrucaoCurvaService.construir` checa `VersaoCurvaRepository.buscarPorExecucaoCurvaId(executionId)` como primeira linha do método — se essa `executionId` já gerou uma versão antes (reenvio do despacho REST, ou retentativa do orchestrator — ver D1d), não processa de novo. Distinto da guarda de concorrência de 6.8, que cobre corrida ENTRE construções simultâneas da mesma curva/data/momento, não retentativa sequencial da mesma execução)
- [x] 9.8 Garantir que consulta de curva vigente nunca retorne versão em `EM_VALIDACAO` ou `REPROVADA` (já garantido pela query real de `VersaoCurvaRepository.buscarVersaoVigentePublicada`, `WHERE estado = 'PUBLICADA'`)

  **Auditoria desta sessão — bug real e significativo encontrado e corrigido antes de aceitar a entrega do agy**: `promoverVersao`/`reprovarVersao` foram entregues como métodos **privados** de `PublicacaoCurvaService`, anotados `@Transactional`, chamados via auto-invocação (`this.promoverVersao(...)`) de dentro do próprio método público. `@Transactional` do Spring funciona via proxy — só tem efeito quando a chamada vem de FORA do bean, através do proxy; auto-invocação nunca passa pelo proxy, e a anotação é silenciosamente ignorada, sem erro nenhum em tempo de execução. Na prática, a "transação" que devia garantir substituir a versão antiga e promover a nova atomicamente (9.4 — nunca zero nem duas `PUBLICADA` ao mesmo tempo) não existia de verdade: uma falha entre os dois `UPDATE` deixaria a curva sem nenhuma versão `PUBLICADA`. Corrigido extraindo as duas operações para `PromocaoVersaoCurvaService`, um bean próprio injetado — chamada entre beans passa pelo proxy normalmente, `@Transactional` volta a funcionar de verdade. Também corrigidos, no código de teste (não na lógica de produção): 3 testes em `PublicacaoCurvaServiceTest` construíam `CurvaJuros` com uma lista de vértices vazia (`CurvaJuros.de` exige ao menos 1 por design, mesmo tipo de bug já visto e corrigido nas seções 6 e 8).

  Verificado no fim: `mvn test` do reator inteiro (8 módulos) — verde, 86/86 em curve-engine; `VersaoCurvaRepositoryIT` rodado à parte contra SQL Server real, incluindo a cobertura nova de `buscarPorExecucaoCurvaId`.

## 10. Interpolação

- [x] 10.1 Implementar a API síncrona de interpolação sobre versão publicada, sem reconstruir (`POST /curvas/{codigo}/interpolacao`, `InterpolacaoController`/`InterpolacaoService`, novo pacote `com.poccurves.engine.interpolacao`. Lê `versao_curva`/`vertice_curva` já publicados via `VersaoCurvaRepository`/`VerticeCurvaRepository` — nunca chama `ConstrucaoCurvaService`/`CurveBootstrapper`, consistente com D9 do design.md ("interpolação opera sobre vértices publicados, não reconstrói"). Contrato de request/response é o mesmo já fixado em `contracts/openapi/curve-api.yaml` (`InterpolacaoRequest`/`InterpolacaoResponse`) de uma sessão anterior — não inventei um formato novo, segui o que já estava especificado. Delegado ao agy com o contrato JSON exato e as assinaturas reais de repositório copiadas no prompt; auditado, bate exatamente. `mvn test` real: 101/101 em curve-engine, incluindo os 10 testes novos — `InterpolacaoServiceTest` usa os `Interpolador`/`PoliticaExtrapolacao` REAIS do curve-kernel (não mock), só os repositórios JDBC são mock, então a matemática de interpolação é exercitada de verdade; `InterpolacaoControllerTest` usa `@WebMvcTest`+`MockMvc` real para a camada HTTP/JSON)
- [ ] 10.2 Implementar a seleção de versão: corrente, por identificador e por `asOf` (**parcial**: corrente (default, `VersaoCurvaRepository.buscarVersaoVigentePublicada`) e por número de versão explícito via campo `versao` do request (`buscarPorNumeroVersao`, novo método) — implementados e testados reais. `asOf` (seleção por instante histórico) NÃO implementado nesta rodada — o contrato já fixado em `curve-api.yaml`'s `InterpolacaoRequest` também não tem esse campo, então não é uma omissão isolada desta tarefa, é consistente com o contrato existente; adiar até haver necessidade real declarada)
- [x] 10.3 Implementar a seleção de interpolador pela definição, com alternativa explícita sinalizada na resposta (**parcial, mesma ressalva**: o interpolador é sempre o configurado em `versao_definicao_curva.interpolador` da definição vigente na data — `DefinicaoCurvaResolutionRepository.resolverConfiguracaoInterpolacao`, novo método, resolvido via `InterpoladorRegistry`/`PoliticaExtrapolacaoRegistry` reais do curve-kernel, com os 6 interpoladores e as 4 políticas registrados em `InterpoladorConfig`, novo `@Configuration`. A resposta sempre sinaliza `interpoladorUtilizado` (não fica implícito) — mas não existe parâmetro no request para pedir um interpolador ALTERNATIVO ao da definição; `curva-api.yaml` também não tem esse campo. Mesma decisão de 10.2: sinalização sempre presente, seleção explícita alternativa adiada)
- [x] 10.4 Implementar o comportamento fora do intervalo conforme a política, com erro nomeado sob política estrita (fora do intervalo dos vértices: tenta `PoliticaExtrapolacao.taxaEm` real; sucesso vira `status=EXTRAPOLADO`; `PoliticaExtrapolacaoEstrita` (que sempre lança `IllegalArgumentException`, nunca extrapola) e qualquer outra falha de política viram `status=ERRO_FORA_INTERVALO` com `erroMensagem` = mensagem real da exceção — nomeado, não um erro genérico. Testado real com `ESTRITA` e com `TAXA_CONSTANTE` bem-sucedida, nos dois sentidos via os interpoladores/políticas reais do kernel)
- [x] 10.5 Implementar a consulta em lote de prazos, preservando a ordem e isolando prazo inválido (`prazosDiasUteis` é uma lista no request, `resultados` devolve um item por prazo NA MESMA ORDEM pedida — testado real com uma lista de 3 prazos onde o do meio é inválido (fora do intervalo, política ESTRITA) e os outros dois válidos, confirmando que o prazo inválido não afeta os outros nem quebra a ordem)
- [x] 10.6 Implementar o cache Redis com chave contendo o identificador de versão (`CacheInterpolacaoPort`/`RedisCacheInterpolacaoAdapter`, novo pacote `adapter/out/cache`. Chave = `interpolacao:{codigoCurva}|{dataReferencia}|{versaoId}|{interpolador}|{prazo}` — inclui o UUID da versão, então uma versão nova publicada produz uma chave inteiramente diferente por construção (nunca colide com a antiga), satisfazendo o requisito do spec ("entradas anteriores inalcançáveis por construção, sem depender de expiração nem invalidação manual") sem precisar de nenhuma lógica de invalidação explícita. Delegado ao agy com o design de chave/serialização já decidido por mim (nada de descoberta deixada pro agy); auditado: escopo limpo (6 arquivos autorizados, `find -newermt` confirmou), consumidor externo checado (`grep "new InterpolacaoService("` — só `UseCaseConfig` e o teste, nenhum outro lugar quebrado; um `InterpolacaoService` homônimo em `curve-bff` é classe totalmente diferente, sem relação), lógica conferida linha a linha contra a spec, nenhum bug encontrado)
- [x] 10.7 Implementar a degradação para leitura direta do banco quando o cache estiver indisponível (`RedisCacheInterpolacaoAdapter.buscar`/`armazenar` envolvem a chamada ao Redis em `try/catch(RuntimeException)`, nunca propagam — `buscar` cai para `Optional.empty()` (equivalente a cache miss, o código de `InterpolacaoService` já recalcula do banco nesse caso) e `armazenar` só loga aviso e retorna. Verificado com `mvn test` real, não só lido: log real capturado no console — `WARN ... Falha ao buscar chave 'qualquer_chave' no Redis: timeout simulado` / `Falha ao armazenar...` — prova que o caminho de degradação executa de verdade, não é só uma alegação)
- [x] 10.8 Implementar a serialização de taxa e fator como texto numérico (já satisfeito desde a implementação original de 10.1 — `ItemInterpolacaoResultado.taxa`/`fatorDesconto` sempre `BigDecimal.toPlainString()`, nunca `double`; o valor cacheado em 10.6 herda o mesmo formato texto, sem conversão numérica em nenhum ponto do caminho de cache)

  Verificado no fim: `mvn test` de `curve-engine` isolado — verde, incluindo os 6 casos novos de `RedisCacheInterpolacaoAdapterTest` (cache miss, hit, `erroMensagem` com `|` interno preservado pelo split com limite 4, serialização exata, degradação em leitura e em escrita).

## 11. Testes

**Nota de método desta rodada**: seção retroativa — antes de escrever qualquer teste novo, auditei os ~50 arquivos de teste já existentes (escritos ao longo das seções 1 a 10) contra os 20 itens abaixo, lendo o conteúdo real, não só nomes de método. 13 dos 20 já estavam genuinamente cobertos por esse trabalho anterior — citados abaixo com o arquivo exato. Os outros 7 eram lacunas reais, confirmadas lendo o código de produção correspondente antes de escrever qualquer teste (nunca deleguei a descoberta do que testar, só a redação do teste já especificado por mim).

- [x] 11.1 Testar determinismo: mesma construção duas vezes produz vértices idênticos dígito a dígito (lacuna real, preenchida — `CurveBootstrapperTest.montarCurvaPreDeDi1_mesmoInsumoDuasVezes_produzVerticesIdenticos`, chama o bootstrapper duas vezes com o mesmo insumo e compara taxa por `BigDecimal.compareTo`)
- [x] 11.2 Testar o gate de insumo faltante para fixing ausente e conjunto de contratos incompleto (já coberto — `CurveBootstrapperTest.montarCurvaPreDeDi1_listaVazia_lancaException`/`_soInsumosIncompletos_lancaException`, tarefa 4.1)
- [x] 11.3 Testar que nenhum caminho preenche insumo por interpolação, repetição ou default (já coberto — `CurveBootstrapperTest.montarCurvaPreDeDi1_insumoIncompleto_ignorado` prova que um insumo parcial (taxa ou prazo faltando) é DESCARTADO, nunca completado por interpolação/repetição/valor padrão; `InterpoladorRegistry`/`PoliticaExtrapolacaoRegistry` só entram em jogo na seção 10, camada de consulta, nunca na construção)
- [x] 11.4 Testar a contenção: modelo que tenta rede, arquivo, processo, que estoura tempo e que lança exceção (já coberto — `GroovyModeloConstrucaoTest`, 19 casos, seção 7.5/7.6, incluindo os ataques de contenção reais documentados naquela seção)
- [x] 11.5 Testar a importação recusando script que não compila e script que não produz vértices (já coberto — `ImportarModeloGroovyServiceTest`, seção 7.4)
- [x] 11.6 Testar a troca de modelo com o serviço no ar, sem reinício, e a volta ao modelo embutido (lacuna real, preenchida — `ModeloConstrucaoResolverTest.mesmaInstanciaDoResolverAlternaEntreBuiltinEGroovyEVoltaSemReiniciar`: a MESMA instância do resolver despacha pra BUILTIN, depois GROOVY, depois BUILTIN de novo, provando que a troca é só uma decisão por chamada a partir do `tipo()` do modelo, sem estado nem recriação)
- [x] 11.7 Testar a publicação atômica revertendo por falha em vértices e por falha em procedência (lacuna real, preenchida com a ressalva honesta documentada no próprio teste — `ConstrucaoCurvaServiceTest.falhaAoInserirVerticesPropagaExcecaoSemInserirProcedencia`/`falhaAoInserirProcedenciaPropagaExcecaoAposVerticesJaInseridos`: provam que a exceção de cada repositório de escrita PROPAGA sem ser engolida por `construir()` — condição necessária para o `@Transactional` real do Spring reverter no banco. **Não é rollback de banco de verdade**: isso exigiria um teste de integração contra SQL Server real, bloqueado nesta sessão porque não há Podman/Docker rodando localmente agora (`podman ps`/`docker ps` confirmaram nenhum motor de container acessível) — mesma classe de bloqueio já documentada na seção 12)
- [x] 11.8 Testar substituição da versão anterior com vértices preservados e unicidade da versão publicada (já coberto — `PublicacaoCurvaServiceTest.substituiVersaoAntigaAoPromover`, seção 9.4)
- [x] 11.9 Testar interpolação em vértice exato, entre vértices e fora do intervalo nas duas políticas (já coberto — `InterpolacaoServiceTest.testVerticeExato`/`testInterpolado`/`testExtrapoladoComSucesso`/`testErroForaIntervaloComPoliticaEstrita`, seção 10.1/10.4)
- [x] 11.10 Testar interpolação sobre curva importada com o mesmo formato de resposta da construída (lacuna real, preenchida — `InterpolacaoServiceTest.interpolacaoSobreCurvaImportadaTemMesmoFormatoDeRespostaQueCurvaCalculada`: `versaoMock.origemVersao()` estubado como `IMPORTADA`, resposta tem a mesma estrutura/campos de uma curva `CALCULADA` — prova empírica de que `InterpolacaoService.interpolar` nunca ramifica por `origemVersao`)
- [x] 11.11 Testar invalidação de cache por publicação de versão nova (lacuna real, preenchida — `InterpolacaoServiceTest.chaveDeCacheMudaQuandoAVersaoDaCurvaMuda`: mesma curva/data/prazo, duas versões diferentes (UUIDs distintos), captura a chave passada a `cacheInterpolacaoPort.buscar` nas duas chamadas e confirma que são diferentes — mesmo mecanismo implementado em 10.6, aqui testado do ponto de vista do requisito de invalidação)
- [x] 11.12 Testar a recusa de construção e de publicação para definição `IMPORTED` (já coberto — `ConstrucaoCurvaServiceTest.falhaSeImported`, seção 6.3; a recusa de publicação é a mesma checagem, porque `PublicacaoCurvaService.processarPedidoConstrucao` sempre chama `construcaoCurvaService.construir` primeiro — não há caminho de publicação que não passe por essa validação)
- [x] 11.13 Testar a comparação entre modelos, incluindo o caso em que um dos modelos falha (lacuna real, preenchida — `CompararModelosServiceTest.falhaNaConstrucaoDeUmModeloPropagaExcecao`: confirma que `CompararModelosService.comparar` não tem try/catch em volta das duas construções — se o modelo B falhar, a exceção propaga nomeando o modelo, em vez de devolver uma comparação parcial ou mascarar o erro)
- [x] 11.14 Testar cada teste de validação com curva boa e com curva defeituosa construída de propósito (já coberto — `ImplementacoesTesteValidacaoTest`, uma classe aninhada por teste de validação (8 no total), cada uma com casos `aprovado`/`reprovado`/`naoAplicavel`, seção 8.3-8.10)
- [x] 11.15 Testar que reprovação bloqueante impede a publicação e preserva a versão anterior (já coberto — `PublicacaoCurvaServiceTest.fluxoReprovado`, seção 9.3)
- [x] 11.16 Testar que teste de aviso reprovado publica a curva com o aviso registrado (lacuna real, preenchida por mim diretamente (não delegada) — `PublicacaoCurvaServiceTest.avisoReprovadoNaoBloqueiaEPublicaComOAvisoNoEvento`: veredito com um `ResultadoTeste` AVISO+REPROVADO e `aprovadaSemBloqueioReprovado=true`, confirma que a versão é promovida (`estado=PUBLICADA`) E que `curvaPublicadaEventPublisher.publicar` é chamado com o aviso na lista de resultados, não uma lista vazia — a peça que faltava para o campo `warnings` documentado na tarefa 9.6 ter alguma prova de que chega populado de ponta a ponta)
- [x] 11.17 Testar que consulta de curva vigente ignora versão em `EM_VALIDACAO` e `REPROVADA` (já coberto pela query real — `VersaoCurvaRepository.buscarVersaoVigentePublicada`, `WHERE estado = 'PUBLICADA'`, seção 9.8; exercitada contra SQL Server real em `VersaoCurvaRepositoryIT`)
- [x] 11.18 Testar o caso de ausência de curva anterior e de curva importada como não aplicável (já coberto — `ImplementacoesTesteValidacaoTest`'s casos `naoAplicavelSemCurva` para `TesteVariacaoCurvaAnterior`/`TesteComparacaoCurvaImportada`, seção 8.9/8.10)
- [x] 11.19 Testar que limite ausente na definição falha a validação em vez de assumir padrão (já coberto — `BateriaValidacaoServiceTest.testeDesconhecidoLancaExcecao`, seção 8.12)
- [x] 11.20 Testar que falha na execução da bateria nunca promove a versão (já coberto — `BateriaValidacaoServiceTest.testeQueLancaExcecaoNaoPropagaEGeraReprovacao` prova que a bateria converte exceção interna em `REPROVADO` em vez de propagar; `PublicacaoCurvaServiceTest.fluxoReprovado` prova que qualquer veredito não-aprovado chama `reprovarVersao`, nunca `promoverVersao` — as duas pontas juntas fecham o requisito, seção 8.15)

  Delegado ao agy em duas rodadas (10.6-10.8 primeiro, isolado; 11.1/11.6/11.7/11.10/11.11/11.13 depois, 5 arquivos de teste só-adição): descoberta e design (chave de cache, comportamento exato de cada gap) sempre meus, agy só escreveu o código já especificado. Auditado nas duas rodadas: escopo limpo (`find -newermt` confirmou só os arquivos autorizados, nenhum arquivo de produção tocado na rodada de testes), nenhum consumidor quebrado (`grep "new InterpolacaoService("` confirmou só `UseCaseConfig`/teste), conteúdo de cada método novo lido e conferido linha a linha contra a especificação antes de rodar. `mvn test` real (não autorrelato do agy) — verde nas duas rodadas; `mvn clean test` do reator inteiro (8 módulos) — verde, sem regressão.

## 12. Integração

- [x] 12.1 Rodar o motor contra Kafka, SQL Server e Redis locais, construindo a curva PRE de uma data real (**verificado de verdade nesta sessão, ponta a ponta**: subi a infra real via `deploy/podman/up.ps1` (Kafka+SQL Server+Redis+Keycloak, perfil `full`) e `podman compose up --build curve-engine` — o serviço builda, sobe e conecta de verdade: `GET /actuator/health` real devolve `db: UP` (SQL Server), `redis: UP` (versão 7.4.11 real), status geral `UP`; log real mostra `HikariPool-1 - Added connection` (conexão JDBC real estabelecida) e `ModeloCurvaBootstrap` inseriu `PRE_DI1_B3` de verdade em `modelo_curva` (confirmado consultando a tabela direto via `sqlcmd`, não só lendo o log). **Achado real e corrigido nesta sessão, bloqueava até isto**: `CurveBootstrapper` nunca tinha sido registrado como bean Spring — `BuiltinModeloConstrucao` (seção 7.2) o recebe por injeção de construtor, então o contexto Spring não subia (gap pré-existente, nunca notado porque o serviço nunca tinha rodado de verdade fora dos testes até agora); corrigido com um `@Bean` novo em `UseCaseConfig`. **Progresso real adicional, mesma sessão, depois de confirmar com o usuário para seguir o pipeline completo**: subi `curve-api`/`curve-orchestrator`/`function-marketdata-http`/`curve-processor` reais via `podman compose up --build`, todos saudáveis. `definicao_curva`/`versao_definicao_curva` de `PRE_DI` (BOOTSTRAPPED) e `PRE_DI_B3_OFICIAL` (IMPORTED, irmã) semeadas via SQL direto — `POST /curvas/definicoes` real do `curve-api` está bloqueado por um bug de Keycloak já documentado e investigado numa sessão anterior (`grant password` falha com "Account is not fully set up" para todo usuário do realm `curvas`, causa raiz não encontrada; ver `deploy/podman/keycloak-import/README.md`) — fora do escopo deste serviço, então semeei as linhas direto (mesmas que a API real produziria, coluna por coluna conferida contra `db/migration/V1__definicao_curva.sql`). Disparei `POST /api/v1/disparos/manual` real em `curve-orchestrator` (sem autenticação — esse serviço não tem `SecurityConfig`) para `BVBG.086`+`BVBG.028` em 2026-08-21 — **aquisição real da B3 funcionou de ponta a ponta**: `function-marketdata-http` baixou os arquivos reais e publicou 511+1492 blocos reais em `marketdata.prioritaria.v1` (confirmado no log real do feeder, bloco a bloco), e `curve-orchestrator` emitiu `curve.build.requested.v1` real para `PRE_DI` duas vezes (uma por conjunto).

**Bloqueio real, encontrado e majoritariamente resolvido nesta sessão** (usuário autorizou continuar investigando): nenhum dos dois consumidores Java (`curve-processor`'s `IngestaoListener`, `curve-engine`'s `ConstrucaoRequestListener`) processava nada — `lote_ingestao` com 0 linhas mesmo com dado real sentado no tópico, `kafka-consumer-groups.sh --list` (direto no broker, fonte de verdade, não inferência de log) devolvendo vazio. Descartado como causa antes de achar a real: conectividade de rede (`nc -zv kafka 9092` de dentro do `curve-processor` conecta normalmente), config de `bootstrap-servers` (idêntica ao produtor do `curve-orchestrator`, que funciona), saúde do tópico (partições saudáveis, líder eleito, ISR completo).

**Causa raiz real #1 — `@EnableKafka` não é automático nesta versão** (Spring Boot 4.0.8 + spring-kafka 4.0.7): contradiz o comportamento de versões anteriores do Spring Boot (onde `KafkaAutoConfiguration` já traz isso). Confirmado empiricamente: sem a anotação, `jstack` real mostrava ZERO threads de Kafka (nem `KafkaConsumer` chegava a ser instanciado); com `@EnableKafka` explícito em `KafkaConsumerConfig`, o grupo de consumo passou a se formar de verdade. Corrigido em `curve-processor` e replicado em `curve-engine`.

**Causa raiz real #2 — `auto.offset.reset` sem default explícito**: o cliente usa "latest" por padrão, então um grupo de consumo novo começa do FIM do tópico — qualquer mensagem publicada antes do primeiro poll do consumidor é pulada silenciosamente (achado real: 511+1492 blocos reais da B3 já sentados no tópico, `lote_ingestao` continuando em 0 mesmo depois do grupo se formar). Corrigido com `AUTO_OFFSET_RESET_CONFIG=earliest` nos dois serviços. **Com as causas #1 e #2 corrigidas, `curve-processor` consumiu os dados reais de ponta a ponta**: `lote_ingestao` COMPLETO para BVBG.086 (511/511 blocos, 331 pontos gravados) e BVBG.028 (1492/1492 blocos, 696 pontos gravados) — dado real da B3 de 2026-08-21, real na tabela `ponto_dado_mercado`.

**Causa raiz real #3, específica de `curve-engine`**: mesmo com #1 e #2 corrigidas, a thread do listener travava indefinidamente dentro de `OffsetFetcherUtils.getPartitionsToValidate` (confirmado com `jstack` real, thread `RUNNABLE` sem progresso por 6+ minutos) — validação de posição de fetch contra epoch de líder que nunca resolvia. Hipótese de incompatibilidade geral cliente/broker (`kafka-clients` 4.1.2 vs. broker `apache/kafka:3.8.0`) foi **descartada**: `curve-processor` usa exatamente a mesma versão e funcionou; tentei pinar `kafka-clients` para 3.8.0 em `curve-engine` e isso quebrou a compilação (spring-kafka 4.0.7 exige classes só existentes em kafka-clients 4.x — revertido). A causa real: o tópico `curve.build.requested.v1` tinha estado histórico de 12+ dias (volume `podman_kafka-data` persistente de sessões anteriores) — uma partição com log-start-offset≠0 (mensagem antiga já expirada/removida por retenção) parece confundir a validação de epoch do cliente 4.x. **Apagar e recriar o tópico do zero resolveu**: consumidor entrou no grupo, todas as 6 partições atribuídas, offsets resetados para epoch=0 limpo, sem travar.

**Fechado de verdade, mesma sessão**: depois de reconstruir com o `catch (Throwable)` diagnóstico em `ConstrucaoRequestListener.ouvir` (mantido no código — é defesa em profundidade legítima, não só diagnóstico: pega qualquer `Error` que o `DefaultErrorHandler` do Spring Kafka não capturaria, loga antes de repropagar), a causa real apareceu no primeiro log real:

```
java.lang.IllegalArgumentException: prazo duplicado: 7 dias úteis
  at CurvaJuros.de -> CurveBootstrapper.montarCurvaPreDeDi1 -> BuiltinModeloConstrucao.construir -> ConstrucaoCurvaService.construir
```

**Causa raiz real #4, não é bug de infraestrutura — é dado real revelando um gap real de correlação**: `BVBG.086`/`BVBG.028` trazem TODOS os derivativos da B3 no arquivo, não só DI1 — câmbio (`DOLU26`, `WDOU26`), índices de país (`ARBU26`, `CHLU26`...) também aparecem, confirmado consultando `ponto_dado_mercado` direto (696 linhas reais em `BVBG.028`, 12 tickers reais diferentes — a maioria NÃO é DI1 — compartilhando os mesmos 7 dias úteis de vencimento de um DI1 real). `InsumoDI1Repository.buscarInsumosDI1` correlacionava QUALQUER `chave_instrumento` presente nos dois datasets, sem filtrar por família de instrumento — corrigido com `AND chave_instrumento LIKE 'DI1%'` (mesmo prefixo real já usado por todos os tickers DI1 reais vistos nesta sessão: `DI1F24`, `DI1U26`...). Teste novo `InsumoDI1RepositoryIT.ignoraInstrumentoNaoDi1MesmoPresenteNosMesmosConjuntosDeDados` — insere um `DOLU26` real com o MESMO prazo de um `DI1F24`, roda contra SQL Server real, confirma que só o DI1 volta. `mvn test` real (não simulado) — 2/2 verde em `InsumoDI1RepositoryIT` contra o banco real.

**12.1 fechado de ponta a ponta, com prova real, não simulada**: depois do fix, disparo manual real → aquisição real da B3 → Kafka real → `curve-processor` real → `curve-engine` real → `PRE_DI` versão 2, **`estado=PUBLICADA`, origem=CALCULADA, 45 vértices reais**, todos os 8 testes de validação `APROVADO` ou `NAO_APLICAVEL` (nenhum REPROVADO) — confirmado consultando `versao_curva`/`vertice_curva`/`validacao_curva` direto no SQL Server real, não inferido de log. A versão 1 (mesmo dado, antes de eu corrigir os limites de validação que eu mesmo semeei com escala errada — `FAIXA_PLAUSIVEL_TAXA`/`LIMITE_TAXA_FORWARD` com limite "1" em vez de "100", taxa real de DI1 é ~14.5%) ficou `REPROVADA` de verdade pela bateria — **essa reprovação real, auditável em `validacao_curva`, já é a prova pedida por 12.4** (curva com "defeito" nos limites configurados, gate reprovando e preservando o resultado auditável; não fabriquei um cenário separado).

Interpolação (`POST /curvas/{codigo}/interpolacao`) não testada via HTTP real nesta rodada — mesmo bloqueio de Keycloak já documentado (grant `password` quebrado para todo usuário do realm `curvas`), não específico do curve-engine. Cobertura real da lógica de interpolação já existe via `InterpolacaoServiceTest`/`InterpolacaoControllerTest` (seção 10) contra os interpoladores reais do domínio, só não contra este payload HTTP específico.
- [x] 12.2 Confirmar a reconciliação exata da curva construída contra a curva oficial da B3 da mesma data (**verificado de verdade nesta sessão, ponta a ponta**: disparei ingestão real do dataset `B3_CURVA_PRE` (a curva pronta oficial da B3, mesmo endpoint documentado em memória de sessão anterior) via `curve-orchestrator` — real, 274 pontos reais persistidos (`pointsPersisted=274 divergences=0`, idêntico ao valor já visto numa sessão anterior, confirmando reprodutibilidade). **Achado real e corrigido**: a publicação da versão IMPORTADA falhava silenciosamente — mesmo padrão do achado #1-3 de 12.1 (o `DefaultErrorHandler` do Spring Kafka nesta versão não loga por padrão antes de recuperar pra dead-letter). Apliquei o mesmo `catch (Throwable)` diagnóstico em `ProcessarEnvelopeIngestaoUseCase.processar` (curve-processor) e a causa real apareceu: `CurvaNaoMapeadaException: UNMAPPED_CURVE` — `PublicacaoCurvaService.publicarCurvaImportada` (curve-processor) resolve a definição pelo `curveIdOrigem` batendo EXATO com `definicao_curva.codigo` (não por `vinculos_fonte`, convenção diferente da que `curve-engine` usa para BOOTSTRAPPED) — **não é bug de plataforma, foi a minha própria semente de dado usando um código arbitrário** (`PRE_DI_B3_OFICIAL`) em vez do identificador real do dataset (`B3_CURVA_PRE`); corrigido renomeando a `definicao_curva` semeada e o vínculo `codigo_curva_importada_irmao` de `PRE_DI` para bater. Depois da correção: `B3_CURVA_PRE` versão 1 `PUBLICADA`, origem `IMPORTADA`, real. Reconstruí `PRE_DI` de novo (versão 4, `PUBLICADA`) e o teste real `COMPARACAO_CURVA_IMPORTADA` — antes sempre `NAO_APLICAVEL` por falta da curva irmã — rodou de verdade: `APROVADO`, `medida_observada=0.105` contra `limite_aplicado=1.00` (AVISO), confirmado direto em `validacao_curva` real, não simulado. Isso atualiza (sem contradizer) a nota de 4.5/8.10: o roteiro ali proposto — "uma vez pronta a seção 10 [interpolação], este teste pode ser ajustado pra usá-la" — hoje já produz um resultado real e mensurável via `CurvaJuros.taxaEm` mesmo sem essa melhoria, porque o suficiente dos 45 prazos DI1 bate exato com o grid de 274 vértices da B3 nesta data real; a limitação de grid documentada em 4.5 continua real para `ReconciliadorCurva` (comparação exata), só não impede `TesteComparacaoCurvaImportada` de já produzir sinal útil hoje)
- [x] 12.3 Importar um modelo Groovy alternativo, apontar a curva para ele e comparar contra o modelo embutido (**verificado de verdade nesta sessão, contra o sandbox real, não mockado**: os endpoints admin HTTP de importação/comparação (`POST /api/v1/modelos/groovy`/`/comparar`) continuam bloqueados pelo mesmo bug de Keycloak já documentado em 12.1 — não específico deste serviço. Em vez de fabricar um teste isolado, semeei um `modelo_curva` GROOVY real via SQL (`PRE_DI1_ALT_SPREAD`, script real `insumos.collect { i -> [prazoDiasUteis: i.diasUteisVencimento(), taxa: i.taxaAjuste() + new BigDecimal("0.10")] }` — mesma sintaxe já comprovada em `GroovyModeloConstrucaoTest`) e uma nova `versao_definicao_curva` (numero_versao=2) de `PRE_DI` apontando pra ele, depois disparei construção real via `curve-orchestrator` (mesmo disparo manual real de 12.1). Resultado real: `ModeloConstrucaoResolver` despachou para `GroovyModeloConstrucao` de verdade (não `BuiltinModeloConstrucao`) — confirmado por `procedencia_curva.modelo_curva_id` apontando pro modelo Groovy — o sandbox real (contenção de 2 camadas da seção 7.5) rodou o script real contra os 45 insumos DI1 reais, versão 3 `PUBLICADA` com sucesso, e a versão 2 (BUILTIN, publicada antes) transicionou corretamente para `SUBSTITUIDA` (prova real de 9.4 também). **Comparação real, direto no banco** (já que o endpoint HTTP de comparação está no mesmo bloqueio de auth): os vértices da versão 2 (BUILTIN) e versão 3 (GROOVY) para os mesmos prazos reais mostram diferença exata de `+0.10` em toda a curva (ex.: prazo 7 dias: 13.902→14.002; prazo 28: 13.827→13.927) — o spread do script aplicado corretamente, vértice a vértice, sobre dado real da B3)
- [x] 12.4 Publicar uma curva com defeito injetado e confirmar que o gate a reprova, com o resultado auditável (satisfeito com evidência real gerada organicamente em 12.1, não fabricada à parte: a versão 1 de `PRE_DI` — mesmo dado real, antes de eu corrigir a escala dos limites de validação que eu mesmo semeei — ficou `REPROVADA` de verdade: `FAIXA_PLAUSIVEL_TAXA` (BLOQUEANTE) e `LIMITE_TAXA_FORWARD` reprovados contra o limite `1` (1%) quando a taxa real observada era ~14.5%/15.1%. Auditável ponta a ponta em `validacao_curva` real — `teste`, `classificacao`, `resultado`, `medida_observada`, `limite_aplicado`, todos reais, consultados direto no SQL Server. A versão anterior nunca existia (era a primeira construção), então "preservar a versão anterior" não se aplica a este caso específico, mas o mecanismo real (`PromocaoVersaoCurvaService.reprovarVersao`, já testado em unidade na seção 9) é o mesmo)
- [x] 12.5 Documentar em `services/curve-engine/README.md` o ciclo de modelo, os limites da contenção, a bateria de validação e a API de interpolação (README reescrito por mim diretamente — o layout do pacote estava desatualizado (não mencionava `adapter/out/construcao`, `adapter/out/cache`, `CurveEngineSecurityConfig`, nem os dois controllers novos) e a linha 23 antiga afirmava explicitamente "não há cache de interpolação implementado hoje", que ficou falsa depois de 10.6. Cobre os 4 pontos pedidos: ciclo de modelo (BUILTIN/GROOVY, resolver sem estado, troca por tela), limites reais da contenção (2 camadas, achado do gap de `new Socket`, limite de memória não isolável documentado como limitação conhecida), bateria de validação (8 testes, classificação por definição, AVISO vs. BLOQUEANTE) e API de interpolação (seleção de versão, cache com chave por versão, degradação))
