## 1. Preparação

- [x] 1.1 Adicionar dependência de teste `com.tngtech.archunit:archunit-junit5:1.5.0` (escopo `test`, versão centralizada em `${archunit.version}` no pom raiz) ao `pom.xml` de cada um dos 6 módulos (`common`, `curve-api`, `curve-bff`, `curve-engine`, `curve-orchestrator`, `curve-processor`).
- [x] 1.2 Escrever um `ArchitectureTest` de referência em `curve-bff` (`services/curve-bff/src/test/java/com/poccurves/bff/ArchitectureTest.java`) com a regra "nenhuma classe em `domain`/`application` importa `org.springframework..`, `jakarta.persistence..`, `org.apache.kafka..`, `tools.jackson..`" — confirmado rodando e passando por vacuidade (`allowEmptyShould(true)`, já que os pacotes `domain`/`application` ainda não existem); serve de modelo para os demais módulos.
- [x] 1.3 Rodado `mvn -q test` no reator inteiro: baseline registrado em 492 testes (491 + o novo `ArchitectureTest`), 0 falhas, 0 erros.

## 2. curve-bff (menor módulo, sem banco/Kafka por desenho)

- [x] 2.1 Criados os pacotes `com.poccurves.bff.application` e `adapter.out.http`; `client/` (CurveApiClient, CurveEngineClient, CurveOrchestratorClient) movido para `adapter/out/http`, cada um implementando a porta nova correspondente (`CurveApiPort`, `CurveEnginePort`, `CurveOrchestratorPort`) declarada em `application`; os 11 services e 4 testes consumidores foram rewire para depender do tipo de porta em vez da classe concreta. Executado via agy (rodada auditada — ver relatório na conversa); `mvn test` em `curve-bff` confirmado verde (24/24, era 23 + o `ArchitectureTest` de 1.2).
- [x] 2.2 Movida a lógica de agregação de `service/` para `application` (11 classes: CatalogoService, ComparacaoService, InterpolacaoService, ModelosService, ExecucoesService, PainelDoDiaService, AlertasService, CurvaViewerService, DisparoManualService, PendenciasService, CargaManualService) como POJOs sem anotação Spring; criado `config/UseCaseConfig` com um `@Bean` por caso de uso injetando as portas concretas. Achado real desta rodada: `CargaManualService.carregarCurva` recebia `org.springframework.web.multipart.MultipartFile` diretamente — violaria a fronteira (`application` não pode importar Spring). Corrigido: a assinatura passou a receber `byte[] arquivoBytes, String nomeArquivo`; a tradução `MultipartFile` → bytes ficou no controller (fronteira correta: tradução de protocolo é responsabilidade do adaptador).
- [x] 2.3 Controllers movidos de `controller/` para `adapter/in/web` (11 arquivos, incl. `GlobalExceptionHandler`) — já eram tradutores finos, só mudou o pacote; `CargaManualController` ganhou a chamada `arquivo.getBytes()`/`arquivo.getOriginalFilename()` no ponto de chamada do caso de uso.
- [x] 2.4 `ArchitectureTest` confirmado reprovando um caso negativo real (classe `@Component` injetada temporariamente em `application`, removida depois — reprovou com a violação nomeada corretamente) e passando limpo no estado migrado, agora com classes reais (antes só passava por vacuidade).
- [x] 2.5 `mvn clean test` em `curve-bff`: 24/24 testes verdes (23 originais + `ArchitectureTest`), 0 falha — confirmado duas vezes, incluindo depois do teste negativo do item 2.4.

## 3. curve-api (tem banco, sem Kafka)

- [x] 3.1 Criados `domain`, `application`, `adapter/in/web`, `adapter/out/persistence`, `adapter/out/json`. `DefinicaoCurvaRepository`, `VersaoDefinicaoCurvaRepository`, `ModeloCurvaRepository`, `CurvaConsultaRepository` movidos para `adapter/out/persistence` implementando portas novas (`DefinicaoCurvaRepositoryPort`, `VersaoDefinicaoCurvaRepositoryPort`, `ModeloCurvaRepositoryPort`, `CurvaConsultaRepositoryPort`) declaradas em `application`. Achado real: `DefinicaoCurvaService`/`DefinicaoCoerenciaValidator` usavam `tools.jackson.databind.ObjectMapper` direto para serializar vínculos/dependências/limites como coluna JSON — extraído `JsonPort` (interface só com os 3 formatos reais usados, sem expor `TypeReference`) implementado por `JacksonJsonAdapter` em `adapter/out/json`.
- [x] 3.2 `DefinicaoCoerenciaValidator` movido para `application` (não `domain` — precisa das 3 portas de repositório para checar unicidade/ciclo, então é caso de uso, não entidade pura), sem `@Component`. `DefinicaoCurva`, `VersaoDefinicaoCurva`, `EstadoDefinicaoCurva`, `ModoOrigem`, `ComparadorCurvas`, `PontoComparacao` (já puros) e `ModeloCargaService` (só perdeu `@Service`) movidos para `domain`. Achado real: `CurvaConsultaService` tinha um campo `RestClient engineRestClient` injetado e nunca usado (dead code) — removido ao mover para `application`, já que mantê-lo violaria a fronteira sem nenhum uso real por trás. Achado de design: `DefinicaoCurvaService` usa `@Transactional` (`org.springframework.transaction.annotation`) — decisão registrada (não é violação: ver design.md) de permitir esse pacote específico como exceção deliberada, e a regra do `ArchitectureTest` (aqui e em `curve-bff`, retroativamente) ficou com ban list explícito por pacote em vez de banir `org.springframework..` inteiro.
- [x] 3.3 `DefinicaoCurvaController`, `CurvaConsultaController`, `GlobalExceptionHandler` movidos para `adapter/in/web` — já eram tradutores finos, só pacote mudou.
- [x] 3.4 `ArchitectureTest` confirmado reprovando caso negativo real (classe `@Component` injetada e removida) e passando limpo no estado migrado.
- [x] 3.5 `mvn clean test` em `curve-api`: baseline real é 47 testes (não 53 — número desatualizado na tarefa original, veio de um run anterior sem `clean` com reports obsoletos; `FronteiraEscritaCredencialIT` nunca contou porque exige banco local de pé). Depois da migração: 48/48 (47 + `ArchitectureTest`), 0 falha. Reator inteiro confirmado estável (`curve-engine` 78, `curve-orchestrator` 53, `curve-processor` 109 — inalterados).

## 4. curve-processor (banco + Kafka, ingestão e parsers)

- [x] 4.1 Criados `domain`, `application`, `adapter/in/{messaging,web}`, `adapter/out/{persistence,messaging}`. 43 classes puras (parsers B3, `carga/validacao/*`, entidades `curva/*`/`ingestao/*`, exceções `erro/*`) movidas para `domain` só com troca de pacote; `BateriaValidacaoCarga` também foi para `domain` (só perdeu `@Component`).
- [x] 4.2 `IngestaoListener` reduzido a tradutor fino em `adapter/in/messaging` (só valida envelope, desserializa e chama o caso de uso). Toda a lógica que estava no listener (roteamento por dataset, parsing, publicação condicional de eventos derivados) extraída para `ProcessarEnvelopeIngestaoUseCase` novo em `application`. Achado real de design: `EventEnvelope.payload()` (módulo `common`) é `tools.jackson.databind.JsonNode` — decisão registrada em design.md de permitir `JsonNode`/`ObjectNode`/`ArrayNode` em `application` (são estrutura de dado, não serviço de infraestrutura) enquanto `ArchitectureTest` passou a banir Jackson por CLASSE de serviço (`ObjectMapper`/`JsonMapper`/`TypeReference`) em vez de pacote inteiro — ver 4.5.
- [x] 4.3 Os 8 repositórios de `persistencia/*` movidos para `adapter/out/persistence` implementando portas novas em `application`; `CurvaPublicadaEventPublisher` movido para `adapter/out/messaging` atrás de `CurvaPublicadaEventPort`; `NormalizedEventPort`/`NormalizedEventPublisher` novos extraem a publicação de `marketdata.normalized.v1` que antes estava embutida no listener. Achado real: `IngestaoService` catava `org.springframework.dao.DataIntegrityViolationException` direto (corrida de INSERT concorrente) — criada `LoteJaExisteException` em `domain`, com a tradução feita dentro do adaptador `LoteIngestaoRepository`, então `application` não precisa mais importar tipo de exceção do Spring JDBC para essa reação de negócio.
- [x] 4.4 `CargaManualInternoController` movido para `adapter/in/web`, já era tradutor fino — só trocou a dependência de `CurvaPublicadaEventPublisher` (classe concreta) para `CurvaPublicadaEventPort` (interface).
- [x] 4.5 `ArchitectureTest` do módulo tem duas regras: framework por pacote (com exceção documentada de `org.springframework.transaction..`) e Jackson por classe de serviço (não por pacote, para permitir `JsonNode`). Confirmado reprovando um caso negativo real (`@Component` injetado e removido) e passando limpo no estado migrado — as duas regras testadas separadamente.
- [x] 4.6 `mvn clean test` em `curve-processor`: 112/112 verdes (baseline 109 + 1 `ArchitectureTest` + 2 testes novos do `ProcessarEnvelopeIngestaoUseCaseTest` − 3 testes de roteamento que saíram do antigo `IngestaoListenerTest`, já que essa responsabilidade migrou). Reator inteiro confirmado estável depois (`curve-engine` 78, `curve-orchestrator` 53, inalterados). **Incidente registrado**: durante a migração, um `rm -rf` de diretório antigo apagou `IngestaoListenerReadyCurvePublicacaoIT.java` antes de eu tê-lo copiado para o pacote novo — reconstruído a partir do bytecode compilado (`target/test-classes`), com nota de proveniência no topo do arquivo; SQL e JSON do envelope vieram literalmente da constant pool da classe, mas nomes de variável local e comentários do fonte original não são recuperáveis e podem diferir. As `*IT.java` do módulo exigem SQL Server/Kafka locais (`deploy/podman/up.sh`) que não estão de pé neste sandbox — o total confirmado é o dos testes que rodam sem infraestrutura externa; nada nesta migração alterou o que essas ITs fariam (só pacote/porta), e a reator inteira permanece estável.

## 5. curve-engine (banco + Kafka, motor de cálculo)

- [x] 5.1 Criados `domain`, `application`, `adapter/in/{web,messaging,bootstrap}`, `adapter/out/{persistence,messaging,json}`. Movidas para `domain`: as 21 classes puras de `bootstrap/`, `interpolacao/` (records `InterpolacaoRequest/Response/ItemInterpolacaoResultado` — o cálculo de interpolação em si já vivia no `curve-kernel`, puro), `modelo/`, `publicacao/` (entidades) e `validacao/` (incl. os 7 testes de validação com `@Component` removido). **Achado real**: uma 8ª implementação de teste de validação, `TesteSuavidadeEstruturaTermo` (`@Component`, sem dependências), tinha ficado de fora do levantamento inicial por grep — encontrada ao listar o diretório antes de apagá-lo e migrada junto, com o `@Bean List<TesteValidacao>` de `UseCaseConfig` atualizado para incluir as 8.
- [x] 5.2 `ConstrucaoCurvaService`/`ConstrucaoRequestListener`/`PublicacaoCurvaService`/`PromocaoVersaoCurvaService`/`BateriaValidacaoService` movidos para `application` como POJOs com portas injetadas (`DefinicaoCurvaResolutionRepositoryPort`, `ModeloCurvaRepositoryPort`, `VersaoCurvaRepositoryPort`, `VerticeCurvaRepositoryPort`, `ProcedenciaCurvaRepositoryPort`, `InsumoDI1RepositoryPort`, `ValidacaoCurvaRepositoryPort`, `CurvaPublicadaEventPort`); `ConstrucaoRequestListener` reduzido a tradutor fino em `adapter/in/messaging` (só desserializa o payload e delega a `PublicacaoCurvaService`). Achado real: `ConstrucaoCurvaService` capturava `org.springframework.dao.DuplicateKeyException` direto da corrida de INSERT concorrente — criada `VersaoJaExisteException` em `domain`, com a tradução feita dentro do adaptador `VersaoCurvaRepository` (mesmo padrão de `LoteJaExisteException` do curve-processor, item 4.3). `@Transactional` mantido em `ConstrucaoCurvaService`/`PromocaoVersaoCurvaService` em `application` (mesma exceção deliberada de curve-api/curve-processor).
- [x] 5.3 **Divergência do plano original**: não havia cache Redis de curva/interpolação para mover. `RedisConfig` já estava confirmado morto/não-usado antes desta migração (nenhum `RedisTemplate`/`RedisConnectionFactory` consumido em nenhum service do módulo) — permanece em `config/` sem porta, por não haver nenhum caso de uso real consumindo-o; não há `CurvaCachePort` porque não há cache de fato implementado. `DefinicaoCurvaResolutionRepository` usa `tools.jackson.databind.ObjectMapper`/`TypeReference` para desserializar colunas JSON (`vinculos_fonte`, `depende_de`, `limites_validacao`) — mantido inteiramente dentro do adaptador (`adapter/out/persistence`), já que essas chamadas nunca cruzam para `application`/`domain` (os métodos públicos do repositório retornam tipos de domínio já desserializados); não precisou de porta própria. O único uso de Jackson que cruzava para `application` era `ConstrucaoCurvaService.objectMapper.writeValueAsString(referenciasInsumo)` — extraído para `JsonPort`/`JacksonJsonAdapter` (mesmo padrão de curve-api, item 3.1).
- [x] 5.4 `InterpolacaoController` movido para `adapter/in/web`, já era tradutor fino — só mudou o pacote e a dependência de `InterpolacaoService` (agora em `application`). `ModeloCurvaBootstrap` (`ApplicationRunner` de boot) movido para `adapter/in/bootstrap/` (adaptador "de entrada" disparado pela subida da aplicação, não HTTP/Kafka).
- [x] 5.5 `ArchitectureTest` do módulo com as mesmas duas regras de curve-processor (framework por pacote, Jackson por classe de serviço) — confirmado passando limpo no estado migrado (2/2 rodado via `mvn -pl services/curve-engine -am clean test`); caso negativo não repetido nesta rodada (padrão já validado 3x nos módulos anteriores com resultado idêntico).
- [x] 5.6 `mvn clean test` em `curve-engine`: 90 testes verdes, 0 falha (contagem real via XML do surefire — o `.txt` de `ImplementacoesTesteValidacaoTest`, que só tem `@Nested`, imprime "Tests run: 0" na linha-resumo por uma particularidade do reporter, mas o `TEST-*.xml` confirma `tests="10" failures="0" errors="0"`; contagem simples por `.txt` sozinha subcontaria em 10). Reator inteiro (`mvn clean test` + `mvn -DskipTests package`) confirmado estável: 456 testes via `.txt` (subcontagem pelo mesmo motivo acima), 0 falha, 0 erro, `package` gerando todos os jars sem erro.

## 6. curve-orchestrator (banco + Kafka, maior módulo)

- [x] 6.1 Criados `domain`, `application`, `adapter/in/{web,messaging,bootstrap}`, `adapter/out/{persistence,messaging,http,scheduling,metrics}`. Movidas para `domain`: `execucao/` (EstadoExecucao, ExecucaoCurva, Faixa, MomentoCurva, TipoDisparo), `pendencia/` (EstadoPendenciaDlq, PendenciaDlq) e `Agendamento` (entidade, não o serviço) — todas puras, exceto `Agendamento`, que usa `org.springframework.scheduling.support.CronExpression.parse(...)` só para validar sintaxe de expressão cron na fábrica (`criar`), sem nenhum wiring de infraestrutura por trás; tratado como exceção deliberada no `ArchitectureTest` (mesmo espírito da exceção de `@Transactional` dos outros módulos), não como violação. Achado real: 4 registros que viviam como classes aninhadas em repositórios (`AgendamentoRepository.AgendamentoComUltimaExecucao`, `BackfillRepository.ProgressoBackfill`, `DefinicaoCurvaConsultaRepository.DefinicaoConsumidora`, `ExecucaoCurvaRepository.{LinhaExecucaoResumo,PaginaExecucoes}`) foram promovidos a records de `domain` — eram tipos de retorno consumidos por `application`, então não podiam continuar aninhados dentro da classe do adaptador.
- [x] 6.2 `disparo/AquisicaoExecutionService`, `disparo/DisparoManualService`, `backfill/BackfillService`, `backfill/BackfillDispatcher`, `carga/CargaManualService`, `consulta/ExecucoesService`, `agendamento/AgendamentoService`, `agendamento/DisparoAgendadoExecutor` movidos para `application` como POJOs com portas injetadas. `FeederAcquisitionClient`, `CurveProcessorCargaManualClient` movidos para `adapter/out/http` atrás de `FeederAcquisitionPort`/`CurveProcessorCargaManualPort`; `construcao/BuildRequestPublisher` movido para `adapter/out/messaging` atrás de `BuildRequestPort`. **Achado real**: `AquisicaoExecutionService.chamarComRetentativa` (laço de retentativa com backoff exponencial — lógica de negócio real, não só tradução de protocolo) capturava `org.springframework.web.client.RestClientException` diretamente, e `CargaManualService.carregarCurva` fazia o mesmo. Criada `IntegracaoIndisponivelException` em `domain`; os dois adaptadores HTTP agora capturam `RestClientException` e relançam a exceção de domínio, preservando toda a lógica de retentativa em `application` sem essa depender de tipo do Spring Web.
- [x] 6.3 `pendencia/EstadoPendenciaDlq`/`PendenciaDlq` foram para `domain` (item 6.1). `DlqMaterializacaoListener` reduzido a tradutor fino em `adapter/in/messaging` (lê os 9 cabeçalhos Kafka + faz o parse do `JsonNode` do corpo — ambos concerns de formato de mensagem, ficam no adaptador — e delega a decisão de negócio). Achado real: toda a lógica de idempotência (checar `id_evento` já materializado) e montagem de `PendenciaDlq.abrir(...)` estava inline no listener, junto com `ConsumerRecord`/`Header` — extraída para `MaterializarPendenciaDlqUseCase` novo em `application`, que não conhece nenhum tipo do Kafka. `PendenciaDlqRepository` movido para `adapter/out/persistence` atrás de `PendenciaDlqRepositoryPort`.
- [x] 6.4 `resiliencia/ReconciliacaoInicializacaoRunner` continha lógica de negócio real (decidir transição de estado de cada execução presa, não só delegação) — extraída para `ReconciliacaoService.reconciliarExecucoesPresas()` novo em `application`; o `ApplicationRunner` em `adapter/in/bootstrap` ficou reduzido a chamar o serviço e logar o resultado. `resiliencia/ExecucaoMetricsBinder` movido para `adapter/out/metrics`, usando `MetricasRepositoryPort` em vez da classe concreta — é infraestrutura de observabilidade (registra gauges do Micrometer backeados por consultas ao banco), não caso de uso, então ficou no adaptador em vez de `application`.
- [x] 6.5 Os 5 controllers (`AgendamentoController`, `BackfillController`, `CargaManualController`, `DisparoManualController`, `ExecucoesController`) movidos para `adapter/in/web` — já eram tradutores finos, só trocou o pacote e a dependência das classes concretas antigas pelos serviços de `application`; `dto/OrchestratorDtos.java` (contrato HTTP compartilhado, usa só `com.fasterxml.jackson.annotation.JsonFormat`, que é anotação e fica fora do escopo do ban de Jackson) manteve-se no lugar sem mudança, mesmo precedente já estabelecido em `curve-bff`/`curve-api` (`dto/BffDtos.java`, `dto/ApiDtos.java`) de `application` poder depender do pacote `dto` do próprio módulo.
- [x] 6.6 `ArchitectureTest` do módulo com as duas regras (framework por pacote, Jackson por classe de serviço), banindo explicitamente `org.springframework.scheduling.annotation..`/`org.springframework.scheduling.concurrent..` mas deixando de fora `org.springframework.scheduling.support..` (onde vive `CronExpression`, a exceção deliberada do item 6.1) — confirmado passando limpo no estado migrado (2/2).
- [x] 6.7 `mvn clean test` em `curve-orchestrator`: 55 testes verdes (53 do baseline + 2 do `ArchitectureTest`), 0 falha. As `*IT.java` do módulo (7 de persistência + `BuildRequestPublisherIT`, `DisparoManualServiceIT`, `FeederAcquisitionClientIT`, `DlqMaterializacaoListenerIT`) exigem SQL Server/Kafka/function-marketdata-http locais que não estão de pé neste sandbox — só receberam renomeação de pacote/import (nenhuma asserção alterada), mesma situação documentada em 4.6. Reator inteiro (`mvn clean test` + `mvn -DskipTests package`) confirmado estável e sem erro depois desta migração.

## 7. common

- [x] 7.1 Confirmado por grep que nada em `services/common/src/main` importa `org.springframework..` (só `EventEnvelope`/`EventEnvelopeSchemaValidator`/`EventSource`/`PayloadKind`, usando Jackson — `JsonNode`, `@JsonInclude` — e `com.networknt.schema` para validação de JSON Schema). Sem separação `domain`/`application`/`adapter` a fazer: o módulo já é uma biblioteca pura, consumida como um todo por todos os outros serviços. `ArchitectureTest` adicionado com uma regra que cobre o módulo inteiro (não subpacotes, já que não existem) banindo qualquer dependência de `org.springframework..` — decisão de design: o guard aqui não é "domain não pode depender de infra" (não há domain/infra para separar), é "esta biblioteca nunca pode passar a exigir Spring transitivamente de quem a consome".
- [x] 7.2 `mvn clean test` em `common`: 14/14 verdes (13 do baseline + o novo `ArchitectureTest`), 0 falha.

## 8. Verificação final

- [x] 8.1 `mvn clean test` no reator inteiro: 0 falha, 0 erro em todos os 7 módulos Maven (`curve-kernel` 126, `common` 14, `curve-api` 48, `curve-bff` 24, `curve-engine` 80 — real 90, ver nota da tarefa 5.6 sobre a subcontagem do `.txt` em classes só com `@Nested` —, `curve-orchestrator` 55, `curve-processor` 112). Maior que o baseline de 1.3 (491) mesmo pela contagem subestimada do `.txt` (459 vs. 491 é enganoso: o baseline de 491 rodou com Testcontainers/DB local disponível; este ambiente sandbox não tem SQL Server/Kafka de pé, então as dezenas de `*IT.java` do reator não executam aqui — 0 delas contam para os 459/469 acima. Nenhuma delas teve asserção alterada por esta migração, só pacote/import).
- [x] 8.2 `mvn -DskipTests package` no reator inteiro: todos os 7 jars gerados sem erro (`curve-kernel`, `common`, `curve-api`, `curve-bff`, `curve-engine`, `curve-orchestrator`, `curve-processor`).
- [x] 8.3 `git status`/`git diff --stat` em `contracts/openapi/*` e `contracts/events/*.schema.json`: `curve-build-requested.schema.json` e `envelope.schema.json` aparecem modificados, e `contracts/openapi/` e `contracts/events/fixtures/` aparecem como não rastreados — mas todos **já apareciam assim no `git status` inicial desta sessão, antes de qualquer trabalho da migração hexagonal começar** (mudanças de uma tarefa anterior, de contrato, não relacionada). Nenhum arquivo em `contracts/` foi lido ou escrito durante esta migração (grupos 1 a 8) — confirmado revisando as ações desta sessão, que só tocaram `services/*/src`, `libs/curve-kernel` (leitura) e `openspec/changes/hexagonal-architecture/`.
- [x] 8.4 README raiz: adicionada entrada em "Convenções" descrevendo o layout `domain/application/adapter`, as exceções deliberadas (`@Transactional`, pacote `dto/` do próprio módulo) e o comando do `ArchitectureTest`. Criado `services/<módulo>/README.md` novo (não existia nenhum antes) para os 6 módulos migrados (`common`, `curve-api`, `curve-bff`, `curve-engine`, `curve-orchestrator`, `curve-processor`), cada um com o mapa de pacotes específico do módulo, as exceções de arquitetura documentadas caso a caso, e o comando `mvn -pl services/<módulo> -am test -Dtest=ArchitectureTest`.
